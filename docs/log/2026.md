# Session log — 2026

Append-only. One entry per working session, newest at the bottom. This is the
archive: *why* something was done, what it cost, and what to watch out for next
time. Nobody needs to read it to start work — that is `docs/STATE.md`.

Entries before 2026-09-02 lived in `docs/HANDOFF.md` §10 and were moved here
unchanged when that file was retired (Decision 74).

---

### 2026-08-02 — handoff verification & doc reconciliation (PR #1)

First session run from the restructured repo rather than from project knowledge. **No application code was touched.** The purpose was to confirm the handoff actually lands cold, and it does.

**Verified, not assumed:**

- `npm run verify` → **20 passing, 2 todo, 0 failing**, matching §4 exactly. Requires `npm install` first — `node_modules` is not committed.
- Both `todo`s are the right two. A1 fails with the precise signature §5 records (`36 !== 0` — six `[data-spec]` + six `[data-aber]`).
- Every finding spot-checked against source is real and at the cited line: **B2** `engine.js:201`, **B7** `engine.js:579`, **B1** `app.js:493`, **B5** `app.js:21`, **B4** `engine.js:317`.
- `index.html` is a 31-line shell with script order data → icons → engine → ui, as §3 claims.
- CI (`.github/workflows/verify.yml`) confirmed working end to end — green on `pull_request` and on the post-merge push to `main`.

**Corrected — four stale facts, all in docs:**

- `SCHEMA.md` header read schema `0.3`; Decision 53 bumped it to `0.4`.
- `SCHEMA.md` §1 architecture table still described `index.html` as the whole app; Decision 54 split it into `src/`. Table now lists the six real source files.
- `SCHEMA.md` §1 ship path described manual inlining; it is `npm run build` now.
- Decision count read 53 in three places (this file ×2, `docs/README.md`); §4 lists 54.

No decision number assigned — reconciling docs to Decisions 53 and 54 is not itself a decision.

**Standing caution for the next session:** the drift above was all in the direction of *docs lagging the code*, and `SCHEMA.md` was stale in its own summary line while remaining the designated authority. Spot-check the claim you are about to rely on. Line numbers in §5 were accurate; the version stamps were not.

**Unrelated note:** `index.html` loads three typefaces from Google Fonts. `tools/build.mjs` inlines *local* assets only, so a GM opening the folder offline gets system-font fallback. Not a defect — flagged because "hand a player a folder and it works" is load-bearing and the typography is brand.

**Position unchanged.** Phase 4 sequence stands as §7 describes it: clear F8, build `picks`/`excludes`/`requires`, retrofit A3, then Biomech. Quick wins B2/B4/B5 remain mechanical and unclaimed; **B1** touches player-facing copy and wants a voice pass rather than a straight deletion.


### 2026-08-29 — CRB v4 content pass: Skills, Advantages & Disadvantages

**No application code was touched.** Data and docs only. `npm run verify` ran before the first change and after every batch: **20 passing, 2 todo, 0 failing** throughout, matching §4.

Merged from the CRB sources — `042_Skills.docx`, `043_Advantages.docx`, `044_Disadvantages.docx` — as Decisions 55-58.

**What moved:**

- **Skills 34 → 36.** New: `occult-lore` (INT/COOL), `survival` (BOD/INT). Recategorised: `tactics` general → combat, `streetwise` utility → general. Now 11 combat / 9 utility / 16 general. Every skill carries a `flavorLine`; eight carry `notes`; Martial Arts carries `styles`.
- **Advantages 57, Disadvantages 30 — every id preserved.** No renames, no removals, so no `migrate()` step and no risk to saved `.shadows.json` files.
- **Numeric changes:** Poverty 2→4 CP and Max Rank 1→3; Gullible 3→5; Passive 3→4; `universal: true` added to Followers/Minion and Time Sense.
- **Game data 0.2 → 0.3.** Verified empirically: a new character stamps `gamedataVersion: "0.3"`, and a 0.2 character reports the mismatch through `versionCheck`. Character schema stays 0.4, untouched.

**Mechanical drift worth knowing about at the table.** Most of the rewriting is voice, but real mechanics moved inside it. One change is systemic and clearly deliberate: **absolute Target Numbers became relative TN modifiers** across nine entries (Iron Will, Machindo, Animal Ken, True Faith, Weak Willed, Fanatic, Terminal Disease, Berserker, Addiction). Individually: Field Medic and Hyper Vigilance swap kicker-die *escalation* for a flat stacking die; **Favored Skill inverts whether LUCK may modify the re-roll**; Coward drops to Target 9 / Threshold 2 with a per-turn retry; Long-Lived now grants Milestones and is creation-only; Machindo adds Pilot; Blood Lust moves a natural 1 from auto-fail to botch; Terminal Disease gains a death clock.

**Fixed in passing by the CRB text:** `time-sense`'s stored description was truncated mid-word ("once per game sessio"); `lightning-calculator` referenced "Advanced Tech", a skill that does not exist (now Engineering); Ghost TAG referenced "Electronic Security" (now Security); Dwarf, Giant and Hemophiliac said `BODY`.

**Flags:** **F10 closed outright** — both halves. The catalog gained Occult Lore and Survival, *and* the Professional subtype references were renamed to match, which mattered because focused-skill matching is by **name**, not id. **F5 is three-quarters closed**; only Cyber-Prophetical remains, waiting on F6. Opened **F15** (Tracking) and **F16** (Hemophiliac) — see §6.

**One real test-suite defect found and fixed.** The "every skill references a stat that exists" guard looped over a `synergy` array that the data does not have, and never checked `synergyStat` — the field `skillLine()` actually reads. It had therefore never guarded the synergy half at all, and would not have caught either invalid synergy stat this pass turned up. One-line fix; suite still 20/2/0.

**Two things the source docs are still carrying:**

Both were resolved at source the same day and re-verified against the saved docs:

- **Tracking read "(INT / INT)"** — a slip made while correcting Occult Lore and Survival off their derived-attribute synergies. Ken confirmed **INT/EMP** and fixed the CRB. The data had carried INT/EMP throughout, so F15 opened and closed without a data change.
- **Age read "BODY Max: 5"** while Dwarf, Giant and Hemophiliac had been corrected to `BOD`. Fixed in the CRB.

The first read of `044_Disadvantages.docx` missed the Age fix because the file was still open in Word — the edit had not been flushed to disk, and its mtime was unchanged. Worth remembering: **re-extract the sources after the author says they are saved and closed**, and diff, rather than trusting the first read. Doing that here confirmed the committed data matched the saved docs with zero differences.

**Deferred on purpose.** Rank tables render as labelled bullets rather than structured `rankTable` (Decision 57), and none of the `picks` machinery the CRB now specifies is encoded (Decision 58). Both belong with the Phase 4 renderer work, not in a content merge.

**Position unchanged.** Phase 4 sequence still stands: clear F8, build `picks`/`excludes`/`requires`, retrofit A3, then Biomech. What changed is that step 2 now has a written requirement set and a ~15-entry test corpus instead of a sketch.


### 2026-09-02 — quick-win pass: B2, B4, B5

The first application-code change since the restructure. Three named audit
findings, no design input needed, no schema bump, no data change, no id moved.
`npm run verify` before: **22 passing / 2 todo / 0 failing**. After: **26 / 2 / 0**.

**Closed as Decisions 59-61.**

- **B2 — stat aliases.** Targets corrected to `MOB`/`MAG`, *and* `normStat`
  now verifies that its alias target is a live stat id.
- **B4 — `undoIP`.** Deleted from the engine and its export list, with a
  comment at the site naming what superseded it.
- **B5 — review step number.** `n: D.creationFlow.steps.length + 1`.

**B2 was worse than two rounds of documentation recorded.** The audit said a
bad alias "would still trip the unknown-stat flag"; the 2026-08-02 re-check
said the targets don't exist. Neither noticed that `normStat` returned the
alias target *without checking it*, so an unresolvable alias came back truthy,
`skillLine` skipped its `dataWarning` branch and dereferenced `t["MA"].value`.
Confirmed by running it: `TypeError: Cannot read properties of undefined
(reading 'value')`. A skill authored against "Movement" or "Attractiveness"
would have taken down the whole Skills tab, not shown a gold flag. This also
means **Decision 22's stated guarantee — "degrades gracefully on unknown ids
instead of crashing" — was untrue for precisely the case it was written for.**
It is true now.

Worth keeping in view: the alias map is the *pre-Shadows* stat vocabulary
(`BODY`, `REF`, `INT`, `TECH`, `COOL`, `EMP`, `MA`, `ATTR`). The two broken
entries had the old system's own abbreviation on the target side — a
copy-of-the-source-column slip, not a guess at a Shadows id.

**Four new guards, each mutation-tested.** Every one was run against the
pre-fix code to confirm it actually fails there — a guard that passes both
before and after is decoration. The alias guard reads the `STAT_ALIASES`
literal out of `engine.js` rather than restating it, so an alias added later
is covered without touching the test.

**No data currently exercises any alias.** Every `primaryStat`/`synergyStat`
in `shadows-data.js` is already a canonical id, and git history shows `ATTR`
and `MA` were never stat ids in this repo. The map is pure tolerance for a
designer typing a long name — which is exactly why a silent throw in it could
have sat there indefinitely.

**One doc drift corrected in passing.** §4 claimed 20 passing; the real count
was 22 — PR #4 added two build-entrypoint tests and the docs were not updated.
Same failure mode the 2026-08-02 session flagged as a standing caution, two
sessions running. §4 now carries the count history so the next drift is visible.

**Position unchanged.** Phase 4 sequence stands: clear F8, build
`picks`/`excludes`/`requires`, retrofit A3, then Biomech. Of the quick wins,
only **B1** remains, and it is player-facing copy — it wants a voice pass, not
a deletion. **B3** and **B6** still want a one-line "is this intended?" first.


### 2026-09-02 — Batch 1: engine totality & CRB conformance

The first batch of the debt-ledger plan. Decisions 62-68. Before: **26 passing /
2 todo / 0 failing**. After: **45 passing / 1 todo / 0 failing** (46 tests).
**A1 is the only `todo` left.**

**Closed:** B3, B6, B7, B8, B9, B10, and the stale F10 flag.

#### The finding that reframed the batch

Three findings raised in three different sessions — **B2** (`normStat` threw),
**B7** (`validate` threw), **B6** (`migrate` left holes the engine threw
through) — were filed as unrelated items in two severity buckets. They are one
defect: **Decision 22 stated a contract and nothing enforced it.** Every time
someone looked, they found another violation.

So Batch 1 fixed the *class*. The contract is now written exactly — every
exported reader is total on anything `migrate()` returns — and a guard runs six
degenerate characters through twenty-seven readers, every `validate` step and
`skillLine`. **It found a fourth violation on its first run** (B10: an orphaned
skill id crashed the review screen, a case `versionCheck` explicitly reports as
supported). That is the argument for the guard, made by the guard.

#### What the CRB settled

`030_Core_Mechanics.docx` was read into the project for the first time, and it
closed **B3** in the opposite direction from the previous session's
recommendation. The engine reproduces both of the rulebook's worked examples
exactly — BOD 4 → 20 HP, and a Werewolf at BOD 11 → 10 HL × 6 HP = 60 HP — so
`levelsPerBOD` was redundant, not undecided. It also exposed **B8**: two floors
the CRB states plainly, sitting in the data as unread prose while the sheet
displayed bare penalties.

That produced **Mechanism 3**: `tests/rules.test.mjs`, where every assertion
quotes the CRB line it pins. **If that file and the CRB disagree, the CRB wins
and the file is the bug report.**

#### Verification, not assertion

- Every guard was **mutation-tested** against the pre-fix code. A guard that
  passes before and after is decoration.
- The data changes were proved inert by **diffing sixty computed outputs** —
  health at six BOD values, pain at four damage levels, milestones at ten MP
  totals — before and after. Byte-identical, which is why no version bumped
  (Decision 68 writes that rule down).
- B6's fix was checked against the original crash reproduction: all four paths
  that threw now return, `gamedataVersion` is *not* masked, and `versionCheck`
  still reports the mismatch.

#### Watch out for this

Writing regexes into test files through a shell heredoc **silently eats one
backslash level** — `\(` becomes `(`, a word boundary becomes a backspace
character. It bit three times this session and every failure looked like a
logic bug. Use the editor for anything containing escapes.

The data also lives in a **VM realm** (`loadEngine`), so its arrays carry another
`Array.prototype` and `deepStrictEqual` rejects them on identity alone. Spread
into a local array first. Both are noted in `rules.test.mjs`.

#### Position

**Batch 2 next** — voice and the `playerNote` / `flagNote` split, both approved.
Do it before Batch 3 so the picks subsystem is authored under the copy standard
instead of retrofitted into it. Full plan, ownership split and the grouped
Deighton ask: the **Shadows Sheet Debt Ledger** artifact.

### 2026-09-02 — Batch 2: app voice & status copy

Decisions 69-73. Before: **45 passing / 1 todo**. After: **51 passing / 1 todo /
0 failing** (52 tests). No schema or gamedataVersion bump — no computed value
moved.

**Ken's diagnosis was right and worth restating**, because it explains the whole
class: the app was built as a demo for Deighton and Scott, so it *narrated its
own roadmap*. A finished app would just have session tracking; this one told the
player it was "arriving in Phase 3". Ten sites in all.

But the second cause was the expensive one: **the data file mixed machine-read
fields, player-facing prose, and maintainer notes with nothing marking which was
which**, and the app rendered the last two down the same path.

#### What changed

- **`flagNote` can no longer render.** `flagHtml()` takes the **entry**, not a
  string, and reads only `playerNote` — passing a raw note is not expressible
  any more. `flagNote` keeps every id, field path and "confirm with D" intact.
- **`playerNote` is optional.** All eleven flags stopped leaking on day one via
  one fallback line; specific copy was written for the four a player actually
  meets during creation (F1, F2, F8, F14).
- **`appCopy`** holds every player-facing string the app generates for a state,
  so **changing the voice is a data edit**. Raw statuses render through
  `statusLabel`, so nobody reads "tbd".
- **F14 moved out of the UI** into `ip`, where every other flag lives. It had
  been a hardcoded `<b>F14</b>` block in the Progression tab.
- **The lock screen lost its fourth sentence** and is better for it. The three
  before it were already a closing cadence landing on "bring it to the table".

#### The one that got away from the manual comb

`validate()` **writes player-facing copy**, and that is where the last
build-state sentence was hiding: *"Cyborg ships as TBD — rules pending."*
Nobody looks for prose in a rules engine. The manual grep missed it; the
enforcement test found it in seconds, because it **renders the app** instead of
reading the source. Its other twenty-odd messages are the best tool voice in the
codebase and were left alone.

#### Testing rendered copy — three things that cost time

1. **`body.textContent` includes the inlined data `<script>`.** The first
   version of the test "failed" on flag notes nobody could see. Strip
   `script`/`style` before reading.
2. **Corpus scanning cannot localise a regression** when three sites render the
   same label — reverting one leaves the others on screen. And banning the bare
   word "draft" fires on the home screen's legitimate "Resume draft". Raw
   statuses are caught by a DOM check on **leaf elements** instead.
3. **The fixture was named "Draft"** and tripped its own assertion.

#### Position

**Batch 3 next** — `picks` / `excludes` / `requires`, then retrofit
specialization (A3), which closes A1 and A2 and flips the last `todo`. The
subsystem will now be authored under the copy standard rather than retrofitted
into it, which was the reason for doing Batch 2 first.

Still open for Deighton, unchanged and still grouped: **F8** (four numbers, the
only wizard-blocker), **F1**, **F2**, **F14** — plus the `statMod` above-10
conflict noted in §5, which still has no F-number.

### 2026-09-02 — docs restructure: HANDOFF retired

Decision 74. No application code touched. Suite **56 passing / 1 todo / 0
failing** (57 tests) — one new file, `tests/docs.test.mjs`.

**Why.** `CLAUDE.md` is the file a cold session reads first, and it was wrong:
"20 passing, 2 todo" when the real figure was 51/1, and "§4 is the ledger; it's
at 54" when the ledger was at 73. Three sessions began from a stale picture.
`HANDOFF.md` had also become two documents in one — 411 lines, of which 245 were
append-only session log, pushing the part you needed below the fold.

**What changed.**

- `docs/STATE.md` — new. Current position and **the batch board**. Rewritten,
  never appended. Capped under 200 lines by a test.
- `docs/log/2026.md` — the session log, moved across unchanged. Append-only and
  never revised: figures stated as-of-a-session are history, and history does
  not drift.
- `CLAUDE.md` — rewritten. **No counts anywhere.** Gained constraints 8 (the
  engine is total) and 9 (maintainer content never renders), which Batches 1 and
  2 established but nothing had written down as a rule.
- `docs/HANDOFF.md` — a stub pointing at the two replacements, so saved
  session-start prompts still land somewhere useful.

Orientation went from ~1,300 lines to ~250. `SCHEMA.md` stays the authority and
is now explicitly *not* read front to back.

**Enforced, not just agreed.** `tests/docs.test.mjs` checks STATE's suite line
against the real count, rejects a count reappearing in `CLAUDE.md`, requires
unbroken decision numbering and that every referenced doc exists, caps STATE's
length, and cross-checks the §5 flag table against `flagged: true` in the data —
the F10 case, where closing a flag in the docs but not the data left it
rendering to players for four days.

**The mistake worth remembering.** The first version of the todo counter read
only the `test(` line. The one real `todo` declares `{ todo: … }` on the *next*
line, so it counted zero — and the script I used to update STATE.md had the
identical bug, so the two agreed and the test passed. **A guard that shares a
bug with the thing it checks will confirm its own blind spot.** Fixed by
matching from `test(` to the callback arrow, plus an assertion that fails if a
`{ todo:` exists anywhere the counter didn't find. All five checks were
mutation-tested afterwards.

**Position.** Batch 3 next — `picks` / `excludes` / `requires`, then A3.

### 2026-09-02 — versioning model, and VOICE-APP adopted

Decisions 75-76. Suite **58 passing / 1 todo / 0 failing** (59 tests).

**The problem Ken named:** no way to confirm he and Claude were looking at the
same build. `package.json` sat at 0.4.0 with nothing reading it, and Batch 2 had
just *removed* the only user-visible indicator — the footer's "app phase 3.3",
correctly, because that was roadmap vocabulary aimed at a player.

**The bigger problem, found while inventorying:** `meta.schemaVersion` meant two
different things. Inside `shadows-data.js` it was the **game data's content
version** (0.3); inside a character file it was the **character schema version**
(0.4). Same name, different meanings, no way to tell which you were reading —
which is the exact confusion the versioning work was meant to remove. The data
key is now `gamedataVersion`, matching the field it stamps onto characters. No
character stores the key *name*, so no `migrate()` step was needed.

**Also wrong:** the data file's versioning comment said to bump only on *shape*
changes. Nobody has ever done that — the CRB v4 pass bumped 0.2 → 0.3 for
content, correctly, and Decision 68 codified that rule afterwards. The comment
now matches practice.

**Four versions, four triggers** — app (a player can see a difference), game
data (computed values or choices can change), character schema (the saved file's
shape changes, always with a `migrate()` step), ruleset (the CRB moves). Rules in
`CLAUDE.md`, values in `STATE.md`, and the app prints its own in the footer:

```
NYTE CITY REGISTRY · GET DANGEROUS GAMES · app 0.5.0 · data 0.3 · CRB v4 (in progress)
```

App set to **0.5.0** — Batches 1 and 2 both changed visible behaviour, so 0.4.0
was already stale.

`tests/docs.test.mjs` fails the build if `APP_VERSION`, `package.json` and
`STATE.md` disagree, if the footer stops rendering the constant rather than a
literal, or if the data file reverts to `schemaVersion`. All three
mutation-tested.

**VOICE-APP.md adopted** (Decision 76) — Ken read it and it landed. Out of
draft, referenced from `CLAUDE.md`, `docs/README.md` and the CHANGELOG, and
enforced by `tests/voice.test.mjs`.

**CHANGELOG** now leads with what a *player* would notice in 0.5.0, with the
engineering underneath — it had been all engineering.

### 2026-09-02 — session close

PRs **#5** (quick wins + Batch 1) and **#6** (Batch 2, docs restructure,
versioning) both merged. `main` at `eb608de`, **58 passing / 1 todo / 0 failing**,
every branch merged and deleted, nothing in flight.

**Where the day started and ended:** 20 passing / 2 todo, with `CLAUDE.md`
telling cold sessions the wrong figures → 58 passing / 1 todo, app `0.5.0`, and
a doc set that fails the build when it lies. Nine audit findings closed (B1–B10
minus the already-closed), three of them raised the same day by guards written
that day.

**The through-line, worth keeping in view:** every problem worth finding today
was a *stated rule with nothing enforcing it*. Decision 22 promised the engine
degraded gracefully — it threw, in three separate places. The flag table said
F10 was closed — the data still rendered it. `CLAUDE.md` said the suite was
20/2 — it was 51/1. The data file's comment said to bump the version on shape
changes — nobody ever had. In each case the fix that stuck was not the
correction but the guard: a test that renders the app, walks the data, or reads
the docs and fails the build.

**Next session: Batch 3.** Read `CLAUDE.md`, then `STATE.md`. Branch
`feat/picks-system` off `main`. `A1` is the only `todo` and it closes with `A3`
inside that batch.


### 2026-09-03 — Batch 3: the selection & constraint system, and A3

Branch `feat/picks-system`. `npm run verify` ran before the first change (**58
passing, 1 todo, 0 failing**) and after every step; it closes at **77 passing, 0
todo, 0 failing** across six files. Decisions 77-81.

**The suite has no `todo` for the first time.** A1 was the last one and it had
been open since the rev 9 audit.

**What was built.** One selection system, hosted by three things — advantages,
disadvantages and skills. An entry declares `picks` (a fixed list, a category, or
free text), `excludes` (symmetric mutual lock) and `requires` (the milestone
prerequisite vocabulary, reused rather than reinvented); a character stores its
answers as `selections` on the entry that asked. Engine gains `optionLock`,
`requirementState`, `picksFor`, `setSelection`, `trimSelections` and the four
`specialization*` readers, all pure and all in the totality guard. `diffChar`
covers `selections` with no extra work, exactly as the audit predicted — checked
by reading `_walk`, not assumed.

All fifteen entries Decision 58 named are encoded, plus Martial Arts styles.

**Three judgement calls worth the ink.**

- **`grants` was split out** into Batch 3b. Decision 58 lists it beside `picks`,
  but they are not adjacent in the code: `picks` adds one reader and two
  validate hooks, while `grants` reaches into `skillPool`, `health`,
  `luckState`, `milestoneState` and a Natural Armor concept the engine does not
  have. Educated is the one that bites — bought at step 7, it grows the step-6
  pool retroactively, which is a wizard-flow ruling and not a data edit.
- **A3 needed no new data field.** The audit proposed `selectMode`. The Arcanist
  already carried `countBy`, and the other four archetypes carried nothing,
  which is "exactly one". Reading the silence was cheaper than editing five
  entries to state it.
- **No entry declares `excludes` or `requires`.** Ken asked for mutual locks and
  the machinery is built and tested, but the CRB names no pair, and writing one
  into the data to exercise the code would be resolving a rules question in
  code. Both are tested against a synthetic fixture pushed into the loaded data
  by the test and popped afterwards. It is on the Deighton list now.

**Two defects found in passing, neither looked for.**

- **A stale mirror.** Professional Natural Advantages live in
  `archetypeChoices.naturalAdvantages` and are mirrored into `ch.advantages`
  with `notes: "natural"`. Changing subtype cleared the ledger and left the
  mirror, so a player switching Cleaner → Enforcer silently kept the Cleaner's
  free advantages. Same two-parallel-models fault as A1/A2, one layer down.
  Fixed in the handler being rewritten anyway (Decision 81).
- **A guard that could not say "all clear".** `tests/docs.test.mjs` checks its
  own todo parser by looking for the `{ todo: }` sentinel anywhere in the test
  sources — and its own assertion *message* contains that sentinel. So the check
  could never pass at zero todos. It had been correct for as long as a todo
  existed, and broke the moment the suite got clean. Excluded from its own scan.
  This is the second time a guard in this file has validated its own blind spot;
  the first is recorded in Decision 74.

**And a third, smaller:** `package-lock.json` sat at `0.4.0` while the app said
`0.5.0`. `npm install` rewrites it from `package.json`, so the drift surfaced as
a stray diff rather than a failure. The version guard now checks both places the
lockfile states it.

**A trap that cost two debugging rounds, now written on the function that
causes it.** `loadEngine()` runs the engine in a `vm` context, so arrays it
returns carry that realm's `Array.prototype` and `assert.deepEqual` — strict, and
prototype-comparing — rejects them against a literal `[]`. The failure prints two
identical-looking empty arrays and says "not equal". The warning is on
`loadEngine()` itself now (Decision 80).

**Ten mutations, ten caught.** Every new guard was run against code with the fix
removed: re-rendering the aberration block, dropping `optionLock`'s reverse
direction, disabling `distinct`, making a GM-approval pick block the lock,
skipping the migrate fold, ignoring `countBy`, ignoring `perRank`, dropping
Martial Arts' `optional`, pointing the sheet back at the aberrations-only path,
and breaking the todo parser. All ten failed as they should.

**Versions.** App `0.5.0 → 0.6.0` (a player sees picks, locks, and one
specialization block where there were two). Game data `0.3 → 0.4` (fifteen
entries now demand an input they did not before — the Decision 68 test).
Character schema `0.4 → 0.5` (one specialization array replaces three fields,
with a `migrate()` step in the same commit). Ruleset unchanged.

**Deferred on purpose.** `src/ui/app.js` was **not** decomposed. STATE said to do
it during the renderer rewrites; Ken's call was to do it on its own branch once
the renderers land green, so a structural refactor is reviewable as a structural
refactor rather than mixed into a diff that also changes behaviour. Noted while
checking: STATE pointed at the 2026-08-02 log entry for the decomposition
mechanism and that entry says nothing about it — the mechanism is in Decision 54.
Pointer corrected.

### 2026-09-03 (later) — adversarial review of PR #7, and what it caught

Ken ran a separate model in a fresh context over PR #7 as an experiment. It
reported eight findings. **All eight were verified against the code before
anything was changed** — the standing rule that another agent's report is a
report, not a fact. All eight were real. Five were defects and are fixed on the
same branch (Decision 82); three are machinery gaps recorded in `STATE.md` §4.

Suite **77 → 85 passing, 0 todo, 0 failing**.

**The one that mattered.** `entryFor` addressed an advantage by id alone, but a
Professional can hold the same advantage on two rows — free from the Natural
Advantages pool and bought with CP. `favored-skill` is in that pool *and*
carries picks, so reads and writes landed on different rows depending on
allocation order, with shipped data, on the day it merged. Fixed by stating the
model: two rows, one trait, ranks summed, purchased row owns the store.

**The rest.** A free-only trait demanded picks with no control to fill them.
`Resume draft` skipped `migrate()` while both other load paths called it.
Decision 81's mirror fix covered the subtype handler and not the two archetype
handlers. `specializationNeed` invented a requirement on a corrupt import.

**A sixth, found while fixing the first:** the natural-advantage mirror is
rebuilt from the ledger on every rank change and threw away the `selections` the
row had just been given. Nothing reported it — it fell out of asking what else
wrote to the row that had just become a store.

**The part worth keeping.** Two guards written from the review's findings
**passed before the fix.** One refused for the wrong reason — the slot did not
exist, so nothing was testing distinctness. The other exercised an engine path
that was already correct, when the bug was entirely in the UI. Both were
tightened until they failed first, and only then fixed. The lesson is not new
but it is now paid for twice on this project: **a finding is not a guard, and a
passing test is not evidence until it has been seen to fail.** Eight mutations
run against the pre-fix code; eight caught.

**On the experiment itself.** Worth repeating. The review had no access to this
session's reasoning, which is exactly why it looked at `entryFor` — a function
this session wrote, tested, and never questioned, because the duplicate-row case
lives in the Professional archetype and every test written for picks used a
single row. The blind spot was structural, not careless. What it did *not* do
was hand over working guards, and three of its eight findings were correct but
not yet actionable. Verify each one, write the failing test yourself, and treat
"no data exercises this" as a real reason to defer.

### 2026-09-03 (close-out) — the map, and four version drifts

Batch 3 merged as **#7**. Two docs PRs followed: **#8** (close-out) and **#9**
(the index). Suite **85 → 86 passing, 0 todo, 0 failing**.

**Why an index exists now.** Ken went looking for what Batch 3b contained, read
`STATE.md` in the wrong checkout, and — even in the right one — would have found
a single board row. The detail was in `SCHEMA.md` §6 the whole time. That is not
a Ken problem: the ledger had passed eighty-two entries, and the ids referenced
everywhere (`A1`, `B7`, `C2`, `F8`) were each defined in exactly one place that
no filename advertises.

`docs/INDEX.md` answers *where is that written down*, in three parts: a routing
table (including the trap that `CHANGELOG` is backward-only and a batch's
reasoning lives in `SCHEMA` §6), an id registry with every audit finding's status
and the decision that closed it, and all 82 decisions in one line each grouped by
topic. The decision lines are generated from the ledger's own opening sentences,
so the index cannot quietly editorialise.

**The guard is the deliverable, not the file.** An index nobody maintains is
worse than none — it sends the next session somewhere wrong, confidently. So
`tests/docs.test.mjs` checks that every decision number in §4 appears in the
index exactly once, and that the open flags in §5 and the registry agree **both
ways**. Closing a flag is now three edits and the build says so. Mutation-tested
in both directions.

**Four version drifts, all found this batch, none of them by looking:**

| Where | Said | Was | Caught by |
|---|---|---|---|
| `package-lock.json` | 0.4.0 | 0.5.0 | a stray diff after `npm install` |
| `README.md` | game data 0.2 | 0.4 | reading it during close-out |
| `CLAUDE.md` version table | app 0.5.0 / data 0.3 / schema 0.4 | all stale | reading it while wiring in the index |
| `tests/docs.test.mjs` todo parser | could never report zero | — | the suite reaching zero todos |

Every one is now guarded. The `CLAUDE.md` case is the sharpest: that file's own
header says it carries no counts *because they drifted here twice*, and its
version table was carrying three. The numbers are gone; the table names where
each version lives instead.

**A near-miss worth recording.** Trimming `STATE.md` under its 200-line cap, a
slice-based edit ran across section boundaries and deleted §3, §4 and §5 — 92
lines instead of about twenty. The length guard flagged the size change, the
section headings confirmed it, and it was restored from the commit and redone
with anchored replacements plus an assert that the set of `##` headings is
unchanged. **Index-based edits to a structured document are a bad idea; anchor on
text and assert the structure survived.** The guard that caught it was written to
stop STATE growing, not to stop it shrinking — it happened to catch both.

**Position.** Batch **3a** (ledger attention state) is next and nothing blocks
it. `renderLedger` marks a passed step done when it has no *errors*, ignoring
warnings, so anyone who walks past Skills with unspent points sees a green tick.
3a also settles the Educated ruling by construction, which is why it goes before
3b. **Thick Skin is held out of 3b**: armor is still in flux on the design side,
and four places in the data already grant Natural Armor in prose, so it wants a
real derived value rather than an early special case.


### 2026-09-04 — Batch 3a: the ledger's third state (Decision 83)

Asked, cold, whether any unblocked architectural work existed beyond the batch
board. It did — 3a was already fully specified in the previous session's
close-out, needed no ruling, and touched nothing but the UI layer, so it was
built rather than just described.

**The fix.** `renderLedger` computed `done` as "no errors" only, so a passed
step sitting on open warnings (unspent Stat or Skill Points, an unresolved
picks slot) rendered the same ✓ as a step with nothing left. `validate()` was
already emitting those warnings for the wiznav issue list below the step — the
ledger just wasn't reading them. Passed rows now split into `done` and `attn`
(gold `!`), and a callout below the list counts how many are in `attn` and
jumps to the first one on click. No engine change, no new validation.

**Guard, mutation-tested.** Added one jsdom test that resumes a draft parked
one step past `stats` with Stat Points left unspent, and asserts the row is
`attn` with a `!`, not `done` with a `✓`. Ran it against the pre-fix renderer
first — it failed with `"got \"step-row  done\""` — before keeping it.

**Versions.** App `0.6.0` → `0.7.0` (minor: new player-visible ledger state).
Neither game data nor character schema changed. Decision 83.

**Position.** Built on `feat/ledger-attention-state`, one commit, `npm run
verify` green (87 passing, 0 todo). Not yet opened as a PR — that needs a
person to say go, and this session didn't have that yet. **Batch 3b (`grants`)
is next** once 3a merges; it still needs Ken's ruling on Educated's step
ordering (§3 of `STATE.md`) before it can be built.


### 2026-09-04 (later) — CRB v4 reference mirror (Decisions 84–85, PR #13)

Ken asked whether the repo should hold a mirror of the CRB v4 WIP itself —
Skills/Advantages/Disadvantages were already merged as data (Decision 55),
but the source prose (mechanics, archetypes, gear) had never been pulled in,
so checking a rules question against current text meant Ken pasting it into
chat each time. Same tradeoff already accepted once for the Voice guide
(Decision 69): drift risk, mitigated by a dated mirror that admits it can go
stale rather than a synced copy pretending it can't.

**Proof before build.** Before proposing a file list, read the sixteen CRB v4
`.docx` files directly off Ken's OneDrive and grepped them against every open
"doc reconciliation" flag in `STATE.md` §3. All of them — F8's flat "3d10+30"
roll, F9's structurally-separate "General Milestones" section, F11's "Intuition
*Advantage*" prerequisite, F16's surviving "First Aid Skill Check", the
beyond-10 "gains slow down" line — still say exactly what the existing
`flagNote`s already recorded. Nothing had drifted, but confirming that had
required a human in the loop every time; that's the friction this removes.

**What landed.** `docs/reference/crb/` — nine chapters (Synergy System, Core
Mechanics, Character Creation, Archetypes, Skills/Advantages/Disadvantages,
Conditions & Recovery, Gear), chosen because each gates an engine formula,
the merged data, or a named open flag. Seven chapters (onboarding, three
encounter chapters, Downtime) were left unmirrored as lower-value until they
actually gate a batch. One shared `README.md` carries the provenance header
— source path via `%OneDrive%` (resolves per-machine automatically; Ken runs
this repo from two workstations) and the re-pull command — instead of
repeating it in all nine files, which is the one deliberate departure from
Decision 69's per-file header.

**Tooling mid-session.** First pass used a hand-rolled regex extractor
(no `pandoc` in this sandbox), which flattened every table to bare line
sequences — readable, but risky on `Gear.docx`'s stat blocks, where a
misaligned row reads as the wrong number attached to the wrong item. Ken
installed `pandoc` between the proposal and the build; re-converted with
`-f docx -t gfm --wrap=preserve` before anything was committed, and it
produces real markdown tables. That prompted checking `GUIDE_Shadows_Voice.md`
too — its 2026-09-02 pull predated pandoc and had silently flattened every
bold, italic and blockquote to plain text. Re-pulled it as Decision 85; no
wording changed, only which characters carry emphasis.

**Versions.** No app, game-data, or schema version change — docs only.
`npm run verify` stayed at 87 passing / 0 todo throughout.

**Position.** Merged as PR #13, `main` fast-forwarded clean. **Batch 3b
(`grants`) is still next** and still waits on the same Educated ruling; this
session didn't touch it.

### 2026-09-05 — `app.js` decomposition, pulled ahead of 3b (Decision 86)

Ken asked whether long-closed decisions and the closed rev 9 audit should be
archived out of `SCHEMA.md`/`docs/audits/`. Reviewed both: the audit already
checks off closed items inline and `INDEX.md` §2 tracks live status per id, so
it needs nothing. The decision ledger is long (~85 entries) but `INDEX.md` §3
already exists to jump straight to a number by topic, so a full archive split
was judged premature — no doc change made. Ken also asked whether Batch 3b
(`grants`) was still blocked on an Educated ruling, remembering that Batch 3a's
attention-state work was meant to settle it. It was: `SCHEMA.md` §6 already
said 3a settled it by construction, but `STATE.md` §5 still carried a leftover
sentence pointing at §3 for a ruling that was never actually listed there —
stale since 3a merged. Fixed as a small doc-drift correction, not a decision.

**What landed.** Decision 54's deferred refactor: `src/ui/app.js` (2,078
lines, one IIFE, ~62 top-level functions) split into `src/ui/shared.js`
(state, persistence, the commit/render loop, cross-cutting render helpers),
`wizard.js` (the eight creation-step renderers), `sheet.js` (the nine
locked-sheet renderers), and a trimmed `app.js` (chrome, event wiring, boot),
loaded in that order after `engine.js`. First proposed a shared-namespace-
object design (each file exposing an API like `Engine` does); dropped it after
mapping every cross-function call and finding almost everything crosses the
wizard/sheet boundary somewhere, including the mutable `S` state object
reassigned wholesale in a dozen places — a namespace split would have had to
export nearly everything anyway, for the cost of renaming `S` at hundreds of
call sites. Classic `<script>` tags in one document already share a global
scope executed in order (confirmed against how `Engine` and
`window.SHADOWS_DATA` already work, and against `tests/harness.mjs`'s
`runScripts: "dangerously"` jsdom setup), so the actual split needed zero
renames — every identifier moved verbatim into whichever file its section
comment already grouped it under.

**Sequencing.** SCHEMA §6 had ordered this refactor *after* 3b, specifically
so it wouldn't need redoing against renderers 3b was about to touch. Went
ahead of 3b instead: 3b was blocked on the Educated ruling (see above), which
this branch never touches, so pulling the refactor forward cost nothing and
meant 3b lands on the new file layout instead of the monolith.

**Verified, not assumed.** `npm run verify` before and after: 87 passing / 0
todo / 0 failing both times, identical count. Also booted the split build in
the real browser tool (`tools/devserver.mjs`) and walked New Character through
power level → concept → stats → Arcanist archetype (the A1-sensitive
specialization/aberration path) with zero console errors, rather than trusting
jsdom alone.

**Versions.** No app, game-data, or schema version change — internal
restructuring only, per Decision 54's own framing. `index.html` gained three
`<script src>` lines (34 total); `tests/build.test.mjs`'s script-order
assertion and asset manifest updated to match.

**Position.** On branch `refactor/decompose-app-js`, not yet opened as a PR —
that's Ken's call. Batch 3b is next and, per the above, genuinely unblocked.

### 2026-09-05 (later) — Batch 3b, `grants` (Decisions 87–88, F17)

`refactor/decompose-app-js` merged as PR #15 first; this session branched
`feat/batch-3b-grants` off the updated `main`. Proposed the design before
writing anything, per house rule: a `grants: [{type, ...}]` array on an
advantage/disadvantage, read fresh every call by a new `Engine.grants(ch)`
that scans held advantages and disadvantages and sums by type — mirroring the
`picks`/`excludes`/`requires` pattern from Batch 3 rather than inventing a
second selection concept.

**What landed.** Educated (`skillPoints`/`perRank`) feeds `skillPool()`; Hard
to Kill (`hpPerLevel`/`perRank`) feeds `health()`'s `hpPer` so it lands on
every Health Level; Lucky/Unlucky (`luckCost`/`spendId`/`delta`) feed
`luckState()`, which needed one small data addition first — a stable `id` on
each `resources.luck.spend[]` entry (`boost`/`explode`), since nothing keyed
off one before; Long-Lived (`milestone`/`kind`/`atRank`) feeds
`milestoneState()`, landing as extra unlocked Minor/Major slots rather than an
auto-picked Milestone, so the existing pick-list UI needed zero changes.
Educated growing the Skill Point pool retroactively at the CP step — the
reason 3a had to land first — needed no new wizard-flow code either: 3a's
attention-state ledger already re-validates a passed step live, so Skills
just starts flagging "attention" the moment `skillPool()` becomes
grants-aware. Thick Skin stayed out, per the standing scope note (armor
design still in flux).

**The one rules call, not resolved in code.** The CRB's Long-Lived table gives
an "Effect" per rank row rather than "gain another," so whether the ranks
stack (rank 3 = 2 Minor + 1 Major) or replace (rank 3 = 1 Major only) was
genuinely ambiguous. Flagged it for Deighton rather than guessing; Ken then
confirmed stacking in this session, which is what got built (Decision 88,
F17) — but it's still marked `flagged: true` pending Deighton's sign-off as
rules authority, since a ruling given in passing during a coding session
isn't the same thing as Deighton's review, and the project's own convention
(`STATE.md` §3) already treats this exact kind of question as his to close.

**Verified, not assumed.** Seven new `engine.test.mjs` cases pin each grant
type, including a floor test (LUCK cost never goes negative) and a "grants()
reads live, nothing is stored on the character" test. Beyond the unit tests,
walked the real render pipeline in a live browser: bought Educated ×2 +
Long-Lived ×3 + Lucky at the CP step and watched the Skill Point pool jump
+20, the Long-Lived flag banner render with zero new UI code, and CP spend
total to 28 exactly as priced; locked the character and confirmed the
Trackers tab showed 7 HP/Health Level (Hard to Kill ×2) and discounted LUCK
costs, and the Progression tab showed "2 Minor (0 taken) · 1 Major (0 taken)"
unlocked from Long-Lived alone, all with zero console errors.

**Versions.** `gamedataVersion` 0.4 → 0.5 (a held character's computed
pools/costs change — Decision 68's rule for exactly this). `APP_VERSION`
0.7.0 → 0.8.0 minor (a player can see a difference). No character-schema
bump. `package-lock.json` resynced via `npm install` rather than hand-edited,
after Batch 3's log entry recorded that manual edits are how it drifted a
full version behind before.

**Position.** On branch `feat/batch-3b-grants`, not yet opened as a PR — 94
passing / 0 todo / 0 failing. Batch 4 (Biomech) is next, waiting on the
design ruling from Ken, Deighton and Scott (F6).

### 2026-09-07 — Printable sheet, demo hosting, and a naming correction (Decision 89, F18)

Ken asked three things that turned out to be one arc: host a public demo of
the app, let a player print or download a character sheet, and add a blank
sheet for physically-only play. Scoped in conversation before any code — demo
hosting as GitHub Pages on a `charactersheet.shadowsrpg.com` subdomain (a
DNS `CNAME`, independent of wherever `shadowsrpg.com` itself lives), the
print/blank paths as one render function since a blank template turned out to
be the same code path with `ch` omitted.

**A naming correction, first.** STATE/SCHEMA/INDEX had "Biomech" creeping in
as an ambiguous alternate name for the Cyborg archetype rewrite (F6). Ken
confirmed the archetype stays **Cyborg** — renamed every forward-looking
reference; left dated log and audit entries alone as historical record.

**A real scope discovery, second.** The two reference character-sheet PDFs
Ken shared have a full Defense panel (Res/Int/Nat/Prot per body location) the
app has never modeled — armor design was known to be "in flux" (Batch 3b's
note on Thick Skin) but had no open flag of its own. Ken then shared
`053_Combat Encounters.docx`, a first real design pass at it: armor rolls
**PROT**, adds static **RES** where the damage type allows (armor-piercing
skips it), and burns a consumable **Integrity** pool. Real direction, not yet
engine-ready — it needs `Gear.docx` for actual item stats, and it's a genuine
new engine subsystem (a mutable tracker, damage-type branching), not a data
edit. Opened as **F18** rather than guessed into the print layout.

**What landed.** `Sheet.renderPrintView(ch?)`: a universal three-page print
layout (Character Info/Stats/Combat/Health/Defense, full Skills, Weapons/
Gear/Advantages/Disadvantages/Notes) styled by a new `print.css` — a separate
white/violet paper theme, letter landscape, independent of the app's dark
on-screen UI. `ch` optional: present, reads the same engine outputs the live
sheet uses; absent, renders labels from `SHADOWS_DATA` alone. Defense prints
blank either way with a plain-language note (F18). A small `Roboto Mono`
build-tag stamp (`app 0.9.0 · data 0.5`) sits under the wordmark on every
page, at Ken's request — visible provenance for a layout expected to keep
changing, and "more tech on the page" besides. Reachable as "Print sheet" in
the sheet header and "Print a blank character sheet" on Home, the latter
needing no character at all. A second build artifact,
`dist/shadows-blank-sheet.html` (`tools/build.mjs`'s new
`buildBlankSheetHtml()`), boots the real app in jsdom and calls the same
`renderPrintView(null)` there, so a standalone download can't drift from what
the live app renders. New `deploy-demo.yml` workflow publishes both `dist/`
files to GitHub Pages on a `v*` tag — same trigger as `release.yml`, so the
public demo only ever shows a tagged build.

**Verified, not assumed.** Rendered both filled and blank print views live in
the browser tool (dev-preview server) by injecting `print.css` unscoped from
its `media="print"` gate for on-screen QA, against a hand-built representative
character. Caught a real bug this way: an `&mdash;` entity piped through the
same `esc()` helper as live values was double-escaping to the literal text
"&mdash;" for every untrained skill's rank — fixed by using the actual dash
character instead of the entity. Opened the standalone
`dist/shadows-blank-sheet.html` directly (no dev server) to confirm it works
with zero app in the loop. `npm run verify` green throughout (96 passing, 0
todo, 0 failing).

**Versions.** No schema bump, no `gamedataVersion` bump — no computed value
changed. `APP_VERSION` 0.8.0 → 0.9.0 minor (a player can now do something
new); bumped everywhere `tests/docs.test.mjs` cross-checks it.

**Position.** Opened as PR #17, four commits (docs naming/F18 · the print
view · the blank-sheet build artifact · the Pages workflow) kept separate on
request, so a problem in one is easy to isolate rather than untangle from a
squash. **Demo hosting isn't live yet** — needs Ken to set GitHub Pages'
source to "GitHub Actions", add the custom domain, and add the Squarespace
`CNAME` record for `charactersheet`. The Shadows-RPG-Site link is deliberately
not added until that resolves. Batch 4 (Cyborg rewrite) is next either way,
still waiting on the design ruling (F6).

### 2026-09-07 (later) — Light theme toggle (Decision 90)

Ken had already built a light theme for `getdangerousgames.com` and
`shadowsrpg.com` — a shared `theme.css`/`theme-init.js`/`theme-toggle.js`
trio — and handed over all three files asking for the same thing here, "so
all 3 sites are in sync."

**Proposed before building, per house rule — this touches `index.html`'s
script list and the CSS architecture.** The shared files' token names
(`--bg-page`, `--card-bg`, `--text-primary`...) don't exist in `shadows.css`,
which names tokens after the brand palette directly and is referenced by
name in ~230 places. Two real options: rewrite the stylesheet onto the shared
names for literal cross-repo parity, or keep this app's own names and port
the toggle *mechanism* only. Asked Ken directly rather than guessing; he
picked the retrofit — port the mechanism, keep the names, since the visual
and behavioral result is identical either way and a ~230-site rewrite buys
nothing a player can see.

**What landed.** `src/ui/theme-init.js` — new, first `<script src>` in
`<head>`, before the `shadows.css` `<link>`, so a stored choice applies
before first paint with no flash. A `:root[data-theme="light"]` block in
`shadows.css` overrides `--ground`/`--panel`/`--panel-2`/`--text`/`--dim` and
folds `--cyan`/`--green` onto `--frame` and `--gold`/`--magenta` onto
`--violet` — the same reassignment the other two sites use, applied to this
app's own token names, since `--frame` (Deep Circuit) and `--violet` (Aether
Pulse) already pass WCAG text contrast on a light ground and the other four
were tuned for the dark ground only. `--ink` flips too, since `.btn.go`'s
text sits on one of the folded (now dark) tokens. The date-input
`color-scheme`/icon-invert pair (Decision 43) got a light-mode override
alongside it — same class of fix, not a separate one.

The toggle button is static markup in `index.html`, next to `#hdractions`
rather than inside it: `renderTopChrome()` clears `#hdractions` on every
screen change (home/wizard/sheet), and the theme preference needs to survive
all three. Wired in `app.js`'s `boot()` with a plain `.onclick`, matching how
every other header control here is wired, rather than as a separate
`theme-toggle.js` doing a one-time `querySelectorAll` — that idiom fit the
other two (static multi-page) sites but would have been dead weight here for
ten lines, since the button never gets re-created.

**Verified, not assumed.** Rendered the toggle live in the browser tool: sun
icon shows in light mode, moon in dark; clicked through home → wizard →
locked sheet with the toggle staying in place and the choice surviving a
reload (`localStorage['shadows.theme']`); checked contrast by computing WCAG
ratios for the four reassigned tokens against `#F5F4F2` by hand (cyan 2.21:1,
green 2.01:1, gold 1.50:1, magenta 4.37:1 — all fail as text; `--frame` and
`--violet` both clear 4.5:1) before deciding they needed folding rather than
their own light values.

**Versions.** No schema bump, no `gamedataVersion` bump — no computed value
changed. `APP_VERSION` 0.9.0 → 0.10.0 minor (a player can now do something
new); bumped everywhere `tests/docs.test.mjs` cross-checks it.
`tests/build.test.mjs`'s script-order assertion and classic-script guard
both updated for the new file.

**Position.** On this branch, not yet opened as a PR — `npm run verify`
green (96 passing, 0 todo, 0 failing). `dist/shadows-character-sheet.html`
picks the toggle up automatically since the build just inlines whatever
`index.html` references; `dist/shadows-blank-sheet.html` was deliberately
left alone, since it's meant to always read as paper. Batch 4 (Cyborg
rewrite) is still next, still waiting on the design ruling (F6) — this
session didn't touch it.

---

### 2026-09-07 — PR #7 machinery gaps closed (Decision 91)

Opened by Ken flagging that the character creator now has real players on
`shadowsrpg.com` and wanting to work down the to-do list toward v1. First
found `docs/STATE.md` itself stale — the light theme toggle session above had
merged as PR #18, but STATE still described it as "not yet a PR" and "main…
through PR #17." Fixed that drift before anything else, since a cold session
reading it would have missed a whole merged batch.

Asked which lane to spend the session on rather than guessing: the three
self-contained PR #7 review findings, the five Ken-alone doc-reconciliation
flags, or something else. Ken picked the engine.js gaps — no design ruling
needed, no player-visible bug, but real debt against Decision 79 (A3)'s
"stop special-casing" goal.

**What landed**, all in `engine.js` unless noted:

- `requirementState` and `majorPrereqs` now share one `checkPrereqs(ch, p)`.
  Before, `requirementState` (advantage/disadvantage/skill `requires`)
  reimplemented `majorPrereqs` (milestone `prerequisites`) by hand and had
  drifted: it never read `majorCount`, `milestones`, `gear`, `note` or
  `gmApproval`, so those fields in a `requires` block were silently treated
  as satisfied. `majorPrereqs` now adds only the "already taken" check on top
  — the one thing that's actually about the milestone's identity rather than
  its prerequisites.
- The Professional stat gate no longer special-cases its own stat loop
  inside `if (a.id==="professional")` — it calls `checkPrereqs` against
  `sub.requires.stats`. Required a data rename: `requiredStats` →
  `requires: { stats: {...} }` on all 8 Professional specialization options
  in `shadows-data.js`, plus the matching read in `wizard.js`'s specialization
  picker. The `if (a.id==="professional")` branch itself stays — Professional
  is still the only archetype with a specialization-level gate — but the
  duplicated stat-check code inside it is gone.
- `heldIds` only scanned `ch.advantages`/`ch.disadvantages`, so a skill could
  never take part in an `excludes` pair even though skills host `picks` the
  same way. It now folds in held skills; `traitName` and the reverse-lookup
  in `optionLock` resolve skill ids too, so both exclusion directions work
  when a skill is on either side.
- `requirementState`'s new prose fields (`gear`/`note`/`gmApproval`) surface
  as a warning in `validate()`'s advantage/disadvantage loop, same pattern as
  a milestone's `manual` prereqs in `sheet.js` — informational, never
  blocking, since the table adjudicates prose requirements, not the app.

None of this is reachable with current data — no entry declares
`excludes`/`requires`/`prerequisites` beyond the synthetic fixtures
(Decision 77), so nothing a player has touched changes. This closes real
machinery debt, not a live bug.

**Mutation-tested, not just passing.** Added two fixture-based tests —
`requirementState` against a `requires: { majorCount, gmApproval }` fixture,
and `optionLock` against a fixture skill with `excludes` in both directions.
Confirmed both actually fail against the pre-fix code (stashed the fix,
reran, watched them fail with the expected assertions, restored the stash)
before trusting them as regression guards — a guard that passes before and
after the fix is decoration, not a test.

**Versions.** No schema bump, no `gamedataVersion` bump — no computed value
changes for any real character; the renamed `requiredStats` field never
produced a different number, only a different key. `APP_VERSION` untouched —
nothing here is visible to a player.

**Position.** On this branch, not yet opened as a PR. `npm run verify` green
(98 passing, 0 todo, 0 failing — up from 96 with the two new tests). Batch 4
(Cyborg) is still next and still waiting on the design ruling (F6); the five
Ken-alone flags in `STATE.md` §3 are still the best zero-dependency session
after this one.


### 2026-09-12 — 2026-09-10 Scott/Deighton design meeting logged (F19 opened)

**No application code was touched.** Ken shared the decisions doc and
readable transcript from the 2026-09-10 meeting (Scott + Deighton; Bill
absent). Read both, cross-checked every mechanical claim against the current
data/schema, and updated the flag table — nothing here was built, since
everything the meeting settled still routes through a flag that was already
blocked on something upstream (`Gear.docx` for F18, the Cyborg design ruling
for F6, Vampire's `status: "tbd"` for F7).

**Folded into existing flags, not new numbers:** F18 now carries the settled
Massive damage formula (strips armor Integrity equal to weapon damage, −1 HL
per 10 points of that damage, +1 additional HL if armor hit zero or was
absent; weapons carry an MD1/MD2/MD3 rating) and a first-pass grenade system
(1X/2X explode ladder at ~10%/~1%/~0.1%; Thunderclap/Shockwave/Blackout;
evasion is a MOB Essence check, not REF). F7 now carries the proposed Vampire
direction (age-scaled feed efficiency, bagged-vs-fresh SFR, vulnerable while
feeding, sunlight cost never fully buys off) and a proposed Werewolf
predator's-mark rework — none of it locked, all of it attributed to the
2026-09-10 meeting so a future session can tell settled from proposed.

**One new flag opened:**

- **F19** — the Cyborg install cost mechanism (Sanity erosion vs. a
  temporary Health Level cost). Scott is explicitly unsure which; blocks
  part of F6's IP-sink design, so it rides Batch 4.

Injured/Maimed (the two named conditions the Massive damage rule references)
did **not** get a flag — see the next entry. The first draft of this session
opened one for them assuming they had no home in the existing Pain Level
system. That was wrong, caught before anything was committed: they're not a
Pain Level variant at all, they're two rows in a full Conditions system the
CRB already defines. Recorded here as a caution, not just fixed silently.

**Deliberately not encoded as flags:** the meeting itself frames most of its
other content as unlocked — the Cyborg install-cost alternatives, the
Vampire/Werewolf proposals above, and everything under "new business"
(Archetypal Mastery milestone, Android archetype, Cyborg-organic symbiosis,
loadable skill programs) are parked, and the action items say Deighton,
Scott, Bill and Ken still need to compare four independent answer sets on
the five archetype questions before anything locks. Inventing flags for
content the meeting notes themselves call unsettled would be resolving a
rules question in code — exactly what this project's working agreement
rules out.

**Versions.** No schema or `gamedataVersion` bump — nothing here changes a
computed value for any character; it's docs only. `npm run verify` unchanged
at 98 passing, 0 todo, 0 failing.

**Position.** Same as the prior entry: Batch 4 (Cyborg) is next and now has
one more dependency (F19) before there's data to encode. F18 has real
formulas; whether `Gear.docx` itself is actually ready turned out to be worth
checking rather than assuming — see the next entry, same day. The five
Ken-alone flags in `STATE.md` §3 remain the best zero-dependency session if
neither Batch 4 nor 4alt is ready to start.


### 2026-09-12 — Gear.docx re-checked: F18 is unblocked, not stale (Batch 4alt opened)

**No application code was touched.** Ken re-attached `Gear.docx`, apparently
believing it was new. A byte-for-byte diff against the mirror already in
`docs/reference/crb/Gear.md` (pulled 2026-09-04) showed otherwise — the two
are identical once a CRLF/LF line-ending mismatch is normalized out. First
diff pass looked like a total rewrite for exactly that reason; worth naming
so a future session doesn't repeat the scare.

**The real finding wasn't "nothing changed."** It was that the unchanged
document is not a placeholder. F18's own text had said `Gear.docx` was needed
"before it's engine-ready" — read in full this session, it turns out to
already be a complete, populated catalog: melee/martial-arts/handgun/SMG/
grenade/rifle/shotgun/sniper/heavy/beam/archery weapon tables with real DMG/
ACC/Range/Cost figures, an ammunition table, three tiers of body armor plus
head/hand gear with real PROT/RES/Integrity numbers, armor features and
upgrades, general equipment (tech/comms, field/recovery chems, tools,
transportation, clothing, food, lodging), vehicles, powered armor, mech
suits, and combat system packages. Nobody had apparently read past the
introduction and table-legend sections when F18 was first opened.

**This also answers F20 before it was ever really a flag** (see the prior
entry's correction). `054_Conditions_and_Recovery.md` — mirrored the same day
as `Gear.md`, so it was sitting there the whole time — defines a complete
18-condition system (Agonized, Bleeding, Blinded, Burning, Deafened,
DeSynced, Disarmed, Disoriented, Frightened, Grappled, **Injured**, **Maimed**,
Paralyzed, Poisoned, Prone, Restrained, Shocked, Stunned, Unconscious), each
with an Effect and a Recovery, entirely separate from the already-built Pain
Level mechanic. Injured is "Body part is damaged and not functional,"
recovering via Focused Healing or a week of downtime; Maimed is "Body part is
gone," recovering only by replacing it. That's a design answer, not a design
question — the gap is that the engine has never modeled Conditions at all.

**What's still genuinely open:** MD1/MD2/MD3 weapon ratings from the
2026-09-10 meeting aren't in `Gear.md` yet — weapons there carry `Siege`,
`Blast (Xm)`, and `Anti-Materiel` tags but no MD shorthand. Small, and doesn't
block building the underlying PROT/RES/Integrity/Conditions machinery.

**Flags.** F18's text corrected in `SCHEMA.md`/`INDEX.md`/`STATE.md` to
reflect the above — no longer describes a missing document, describes a
small remaining gap. No new flag opened; this is engineering scope, not a
rules question, so it doesn't belong in the flag table.

**Not built this session, deliberately.** The scope here — weapons, armor,
Conditions, general equipment, and (probably out of scope for a personal
character sheet) vehicles/powered armor/mechs — is easily the largest single
content surface since the CRB v4 skills/advantages/disadvantages pass, and
touches the engine (a new Conditions subsystem), the character schema
(loadout, active conditions, armor Integrity as current-state), and the
printable sheet's blank Defense panel. Per the project's working agreement,
schema and engine shape gets proposed and agreed before it gets built. Asked
Ken how he wants to scope and sequence it against Batch 4 (Cyborg).

**Versions.** No schema or `gamedataVersion` bump — docs only. `npm run
verify` unchanged at 98 passing, 0 todo, 0 failing.

**Position.** Two real next-session candidates now exist with no external
blocker: Batch 4alt (Weapons/Armor/Conditions, F18) and the five Ken-alone
doc-reconciliation flags. Batch 4 (Cyborg) still waits on Ken/Deighton/Scott
for F6 and F19.


### 2026-09-12 — Weapons, Ammo & Armor merged as data (Decision 92)

Ken confirmed the split from the prior entry — data first, engine second, in
separate sessions — and approved starting the data half immediately. Also
raised, to revisit once this lands: "Batch 4" has meant Cyborg specifically
since Batch 3, and that's started reading oddly now that other work queues
ahead of it. Not resolved this session; noted in `STATE.md` §2 so it isn't
lost.

**What merged.** Seven new arrays in `shadows-data.js`: `weapons` (54, across
melee/martialArts/handguns/smgs/grenades/urbanRifles/shotguns/sniperRifles/
heavyWeapons/four beam subcategories/archery), `ammunition` (9), `arrowheads`
(11), `armor` (37, across light/medium/full body coverage plus head/hand),
and reference glossaries `weaponTagGlossary` (29), `weaponFeatureGlossary`
(6), `weaponModGlossary` (7), `armorFeatureGlossary` (10),
`armorUpgradeGlossary` (6), plus `armorRules` documenting the PROT/RES/
Integrity mechanic as reference text — no dice-rolling code, matching
Decision 11 (rolls are physical). Full shape and reasoning in `SCHEMA.md`
Decision 92.

**Transcription choices, made once and worth naming so a later session
doesn't second-guess them:** flavor text is a one-line hook (`flavorLine`),
matching the skills-catalog pattern, not the CRB's full paragraph — mechanical
fields (damage, cost, tags) are what a character sheet actually needs, and
the full prose stays in `docs/reference/crb/Gear.md`. Tags and Features are
stored as the literal strings the book prints, including per-weapon
parameters like "Area (5m)" — collapsing those into a fixed catalog id is an
engine-batch decision (how does the math actually read a tag), not a
transcription one. Four tags used in the weapon tables (Suppression, Blast,
Anti-Materiel, "Reach" as a tag distinct from the Reach column) have no
formal definition anywhere in the CRB source; each carries `flagged: true`
with a note naming the gap rather than an invented rule.

**Character schema 0.5 → 0.6.** A `weapons` entry is now either a catalog
reference (`id` + `notes`) or freeform (`custom: true`, every field preserved
exactly as the pre-0.6 shape stored it). `migrate()` tags every existing
entry `custom: true` — it does not attempt to match a free-typed name against
the new catalog, because a fuzzy match that guessed wrong would be worse than
an honest freeform tag. A new `armor` field follows the same split, with
`integrityLoss` as current-state input, same category as `trackers.damage`.
Verified with a dedicated migrate test (`engine.test.mjs`) rather than just
running the suite and trusting green.

**One test-writing snag worth recording:** two new data-shape checks
(`assert.deepEqual(bad, [])`) failed with "same structure but not
reference-equal" even though the actual and expected values printed
identically as `[]`. Cause: `.filter()`/`.map()` chained directly on a
game-data array inherit that array's Array constructor from the VM context
`loadEngine()` runs the engine in (see `engine.test.mjs`'s file header) —
a different realm than the test file's own `[]` literal, which
`assert/strict`'s `deepStrictEqual` treats as unequal. Fixed by building the
`bad` list with a plain loop and `.push()` into a same-realm array, matching
every other such check already in the file. Left a comment on the fix so the
next person adding a data-shape guard copies the working pattern instead of
rediscovering this.

**Deliberately not touched:** `src/ui/sheet.js`'s Loadout tab still renders
the old free-text `editTable` — checked that this doesn't break on the new
shapes (`editTable`/`pRowsTableHtml` read `row[col] || ""` generically, so a
`{id, notes}` or `{custom:true, ...}` row renders without crashing, just with
mostly-blank cells nothing in the current UI can populate yet). No Conditions
system, no PROT/RES/Integrity math, no Massive damage application, no
Loadout picker — all engine-batch work, next session or later, per the split
Ken confirmed.

**Versions.** Game data **0.5 → 0.6** (new content — Decision 68's bump
rule). Character schema **0.5 → 0.6** (shape change, `migrate()` step
included). `APP_VERSION` **unchanged** — nothing a player can currently see
is different; the catalog is inert until the engine batch wires it to a UI
(the "don't bump on a no-op" caution from Batch 1, applied to the app version
specifically rather than game data, which does have new content).

**Suite:** 98 → **101 passing, 0 todo, 0 failing** (three new tests: weapon
skill-reference validity, weapon/armor/ammo/arrowhead id uniqueness plus
armor slot shape, and the migrate/custom-tagging behavior above).

**Position.** On this branch, not yet a PR. The engine + Loadout-UI half of
this work (PROT/RES/Integrity math, Conditions, Massive damage, pickers) is
the natural next session and has no external blocker. Batch 4 (Cyborg) still
waits on F6/F19. Batch-naming scheme is an open question for whenever the
board gets touched next (see above).


### 2026-09-12 — STATE.md reorganized by area, not by batch sequence

**No application code was touched.** Ken answered the naming-scheme question
raised earlier the same day: organize by **topic area with a status and a
prerequisite**, the same shape `STATE.md` §3 already used for "who needs
Deighton" but applied to the whole forward-looking half of the file instead
of just the flag table — because different areas (Gear, Cyborg, Vampire,
Arcanist) wait on different people and resolve in whatever order those
people answer, not in a fixed sequence a batch number implies.

**What changed.** `STATE.md` §2 ("The board") is now shipped history only —
a flat table of merged batches, unchanged in spirit from before, just
stripped of the forward-looking rows that used to sit at its tail. §3
("Who can clear what") became **"Areas of work"**: one table, one row per
topic (Gear & Combat, Creation-pool economics, Milestones & doc
reconciliation, Advantages/Disadvantages fine print, Cyborg, Vampire,
Werewolf, Arcanist/Magic, Engine internals), each carrying a status
(✅/⏭/🔶/⏸) and a "waiting on" column that names the person or flag —
folding the old three-way split (Ken alone / Needs Deighton / Design work)
into topic rows rather than person rows, since a topic's owner is now data
in a column instead of the organizing principle. Arcanist/Magic gets a row
with **no F-number** — its open questions come from the 2026-09-10 meeting's
own "not yet locked" framing (subtypes, spheres, implements) and the meeting's
action items already say a four-way comparison has to happen before anything
there can be answered, so inventing a flag for it now would just be a flag
that immediately reads as premature.

**CLAUDE.md got three small wording fixes, not a rewrite.** Asked directly
whether it needed updating for this shift: it didn't need new content,
because it never encoded strict batch sequencing in the first place — the
branch-naming examples were already topic-named (`data/biomech-rewrite`,
`fix/a1-double-aberration`), not batch-numbered. What it did have were three
places that talked about "the batch board" or "batch order" as STATE.md's
organizing principle, which is no longer literally true: the Orientation
section's `docs/STATE.md` line, and the closing "What's next" section (which
also pointed at the now-wrong section number, §2 instead of §3, since the
forward-looking content moved). All three reworded to "grouped by area with
status and what's blocking it," no other content touched. `SCHEMA.md` §6
(Roadmap) needed nothing — checked it directly, and it's pure backward-looking
phase history already, never a forward sequential plan.

**INDEX.md** — one lookup-table row updated: "what's next" now points at
`STATE.md` §3 alone, with a note that §2 is shipped history only.

**Versions.** No schema or `gamedataVersion` bump — docs only. `npm run
verify` unchanged at 101 passing, 0 todo, 0 failing. `STATE.md` is shorter
than before this pass (163 lines vs. 198) despite covering the same ground —
a topic table turned out more compact than the paragraphs it replaced.

**Position.** Unchanged from the prior entry: the Gear & Combat engine/UI
half and the Milestones & doc-reconciliation area are the two candidates
with nothing external blocking them. The Weapons/Ammo/Armor data batch and
this reorganization are still uncommitted, waiting on Ken to say whether/how
to commit and branch it — PR #7's fixes (Decision 91) are **not** part of
that: `git log` shows they merged to `main` via PR #19 before this session
started. `STATE.md`'s board and "Where to start" both still described them
as "done on this branch, not yet a PR" — carried forward from the file's own
prior content without checking it against `git log` first. Caught here,
before anything this session got committed on top of the stale claim;
corrected in the same edit.


### 2026-09-20 — Magic, archetype-independent half merged (Decision 93)

**Started from a review pass, not a build request.** Ken attached Scott's
`Magic.md` WIP (the rework of Evocation/Enchantment/Alchemy, in flight
alongside the archetypes/Cyborg work) and asked what could be locked in
*outside* the archetypes while he reviewed it himself. The doc read cleanly
into two halves: a universal Spellcraft system (the roll, Domains/Glyphs,
spell tiers, Concentration, Sovereign Soul, Charging, the Known/Mastered
progression, Enchantment/Alchemy's materials and time rules) and Origins
(Book/Blood/Bound), which the doc itself places in "the Arcanist chapter" —
exactly the subtypes/spheres/implements question `STATE.md` §3 already had
waiting on the 2026-09-10 meeting's four-way comparison. Proposed merging
only the first half, data-only, in the same shape Decision 92 used for the
equipment catalog. Ken agreed and asked for the game data update.

**A drift check that came back negative, which is itself worth recording.**
While reading the doc, the TOL formula appeared to read INT+BOD+COOL against
Decision 9's INT+COOL+EMP — a real discrepancy if true, so it was flagged
rather than assumed either way. Ken checked directly with Scott: the formula
is INT+COOL+EMP, matching Decision 9 exactly. Nothing in `derived.TOL`
needed to change. Recorded in Decision 93 anyway, because a memory of "we
checked this and it was fine" is worth as much as a memory of a real fix —
otherwise a future session rereading the doc could "correct" a formula that
was never wrong.

**A live mechanical error, found the same way.** The Arcanist's
`coreMechanic.description` said Duds accumulate into a separate Exhaustion
pool, and a Dud that pushes Exhaustion past TOL causes a Rupture. Scott's
doc says a Rupture is a per-roll outcome (Duds exceed Hits *that roll*) and
spends TOL directly, by the Rupture's degree — Exhausted is what a
character is at 0 TOL, not a second tracked resource. Confirmed directly
with Ken/Scott before touching anything. This meant more than a copy edit:
the Arcanist's `exhaustion` tracker panel and the `trackers.exhaustion`
field it was hand-wired to (special-cased in `app.js`/`sheet.js`, predating
and never migrated onto the generic `trackers.panel[id]` mechanism Decision
26/87 established) both had to go. `migrate()` now drops a pre-0.7
character's `trackers.exhaustion` value outright — there was nothing in it
worth preserving under a new name, since the field never tracked what the
corrected rule needs tracked.

**What merged as data:** `domains` (5), `spells` (96 — 18 Cantrip/47
Standard/17 Advanced/14 Superior across the 5 domains), `spellTiers`,
`spellcraftRules` (reference text, same role `armorRules` plays — no dice
roll here either, Decision 11), `enchantmentMaterialCategories`/
`enchantmentTimeTable`/`enchantmentExtendedTime`, and `spellTagGlossary`
(one entry, `AP`, `flagged: true` rather than assumed — it's on Iron Lance
but not in the WIP's own tag list, same pattern Decision 92 used for
undefined weapon tags). Overflow text keeps the WIP's own `[X]` placeholders
verbatim; those are unresolved balance numbers, not a transcription gap, and
none was invented to fill one. The Grimoire panel gained an `Overflow`
column.

**Verified, not assumed.** `npm run verify` before (101/0/0) and after
(103/0/0 — two new tests: spell/domain referential integrity, and
`migrate()` dropping the retired tracker). Then booted the split build in
the real browser tool (`tools/devserver.mjs`) and walked New Character
through power level → concept → stats → Arcanist archetype → CP step → lock,
confirming in the rendered sheet (not just the data file) that the corrected
`coreMechanic.description` and reworded Discipline text render on the
Archetype tab, the Grimoire table on Loadout & Powers shows the new
`Overflow` column, and the Trackers tab no longer has an Exhaustion row —
with zero console errors and the footer correctly reading `app 0.10.1 ·
data 0.7`.

**Versions.** Character schema **0.6 → 0.7** (the `trackers.exhaustion`
field is gone). Game data **0.6 → 0.7** (the new content plus the corrected
description). App **0.10.0 → 0.10.1**, patch: an existing Arcanist draft
renders a different tracker panel and different Magic/Discipline copy — a
player-visible difference even with the archetype still `status: "draft"`.

**Deliberately not built this session:** Origins/subtypes (still blocked,
see above), wiring the Grimoire table to reference the new `spells` catalog
by id (still free-entry — an engine+UI batch, the same split Decision 92
drew for the Loadout picker), and the Tools of the Trade pricing tables
(every row is still `[X] Ç` in the WIP).

**Position.** Arcanist/Magic in `STATE.md` §3 is no longer fully blocked —
the archetype-independent half is in, and wiring the Grimoire to the new
catalog is available as engine/UI work without touching Origins. The
Weapons/Ammo/Armor batch (Decision 92) and this batch are both done on this
branch, not yet a PR. Cyborg and Vampire are still the areas waiting purely
on people outside this session.

### 2026-09-21 — Print sheet: a real bug, a visual redesign, and Scott's real assets (Decision 94)

**Started from a bug report.** Ken printed a blank character sheet from the
demo site and got the intake wizard's screen instead of the sheet.
`shadows.css` (all media) hides `#printSheet` with `display:none`, and
`print.css` — `media="print"`, so only its own rules are print-only — never
set it back to visible, and never hid the rest of the app chrome either.
`window.print()` was faithfully printing whatever was on screen. Fixed by
un-hiding `#printSheet` and hiding `#app`/`#vscrim`/`#vdrawer`/`#footer`
inside `print.css` itself. Caught by inspecting the live cascade after
clicking the button, not by assertion.

**Then a design conversation, not just a fix.** Ken wanted the printed
sheet — 3 pages of dense tables — easier to parse, and asked whether Claude
Design (the canvas/prototype Artifact type) was the right tool. Verdict:
no, for iteration on the *existing* print CSS — a Design mockup would still
need hand-translating into `print.css`/`renderPrintView()`, an extra step
with no accuracy gain when the real code and a live preview were already
available. Design earned its keep once the ask changed to genuine visual
restyling with no existing implementation to anchor to: three low-fi
direction artboards (Case File / Tactical Grid / Folio Panels), all sharing
the existing brand system, explored there before any real code changed.
Ken picked Case File.

**Then Scott's real Affinity files, not an invented look.** Ken had a
folder of Scott's reference character-sheet PDFs (Professional/Arcanist/
Cyborg) and, later, the actual `.afdesign`/`.afpub` source. Both are
Serif's proprietary binary format — no parser exists outside Affinity, so
they can't be opened directly. What worked: `.afpub` caches a PNG OS-preview
thumbnail at the tail of the file, extracted by scanning for the PNG magic
bytes with no library needed at all — that's how the frame's silhouette was
first seen. Ken then installed Affinity (free as of late 2024) and
exported real SVGs: one of the background texture (a hex-cluster/circuit-
trace motif, exact paths and colors), and two more of a page spread with the
real notched frame and a WIP Stats section. The frame's exact chamfer/notch
geometry was pulled out via the browser's own transform math (`getCTM()` +
raw `M`/`L` coordinates), not eyeballed off a raster.

**A real mistake, caught before it shipped twice.** First: using the
frame's polygon as a CSS `clip-path` directly on `.p-page` silently deleted
the "SHADOWS" wordmark — the notch beside it isn't empty space cut from a
rectangle, it's where a separate protruding tab sits in Scott's design.
Switched to a non-destructive stroke-only SVG background overlay instead,
which can draw a line but can never hide content. Second, smaller: the
first cut of Pain-Level-banded Health Levels gave each band its own labeled
row, which alone added ~190px and pushed the front page onto a second
printed page — precisely what "page 2 is completely blank" turned out to
be (overflow spillover, not a missing render), plus it pushed every later
page one sheet further, which is what made a pre-existing ~70px overflow on
the Loadout page suddenly visible as the Notes box clipping across a page
break. Both fixed by measuring actual rendered heights against the true
print budget (980×740px content area at Letter landscape) rather than
guessing, then trimming until every page cleared it with real margin — a
flat single-row health ladder grouped only by gap and border weight, and
tightened table/notes padding on Loadout, no blank-row counts cut.

**What's still open, and why work stopped here.** The frame/texture layer
renders correctly on screen (confirmed by force-enabling `print.css`
outside its `media="print"` gate) but not in Chrome's actual print/PDF
output — likely a print-pipeline quirk specific to `background-image` on
generated `::before`/`::after` content, since the plain `background-color`
header bars print fine. Not chased further: Scott's exported file is
itself a two-page **portrait** WIP with only the Stats section built, in
two competing styles (a vertical hex-dial sidebar vs. a plain ruled table)
— Ken's call was to stop hand-translating fragments and wait for a
complete, finished export, at which point portrait-vs-landscape becomes a
real decision rather than an assumption baked in from a partial file. The
current landscape layout and Scott's real border/texture assets ship as-is
in the meantime, print bug included in the fix, frame-in-print bug flagged
open.

**Verified.** `npm run verify` stayed 103/0/0 throughout — no engine, data,
or schema touched, UI/print-rendering only. Measured real page heights in
the browser after every pagination-affecting change rather than trusting
that a change "looked shorter." Confirmed the actual `media="print"`-gated
`index.html` path (not just the always-on standalone `dist/` build) renders
the same result by temporarily flipping the stylesheet's `media` attribute
live.

**Versions.** No game-data or character-schema change. App **0.10.1 →
0.11.0** (minor): a materially different look for an existing capability.

**Position.** Print sheet visual work in `STATE.md` §3 is not blocked on
anyone to *continue* — Ken is reaching out to Scott for a complete export —
but a portrait rework and the print-only frame bug are both real, named,
open items rather than silently-broken or silently-abandoned ones.

### 2026-09-22 — Level set: CRB mirror re-pull, F16 closed, combat plan written

**Why.** Ken asked for a level set — where the app stands, and what can be
nailed down without the archetypes finished. The honest answer started with
the mirror: `docs/reference/crb/` was pulled 2026-09-04 and the CRB had moved
under it. A whitespace-insensitive diff showed real change in Core Mechanics
(the Exhaustion/TOL section rewritten to the Rupture-spends-TOL model the data
already follows since Decision 93), Archetypes (the Arcanist's Book/Blood/Bound
Origins drafted, dated the same day), and Gear (a Suppression rule). Five
chapters that now gate work were not mirrored at all.

**What changed.** All nine mirrored chapters re-pulled; 053 Combat, 055
Downtime, Magic, the Book of Known Spells and the new Aberrations appendix
added. The spell catalog was checked against the live appendix by script: all
94 entries the parser recognised match on name, TN and TH, and the two it
missed (Counterspell, Silence) are present in a different layout. Ken
corrected Hemophiliac in the CRB mid-session (First Aid → Medical), which
closed **F16**: data text re-synced, flag dropped, SCHEMA §5 and INDEX rows
removed. No `gamedataVersion` bump — text only (Decision 68). The flag-count
sentence in SCHEMA §5 said twelve; a recursive count says sixteen, so it was
already stale before this change.

**The plan.** `docs/plans/combat-and-conditions.md` (a new `docs/plans/`
folder) lays out F18's engine half together with the Conditions system 054
now fully specifies, as three build sessions plus an independent Magic-tables
side session. It is a proposal (P1–P9) awaiting Ken's sign-off, not decisions
— those get numbered as each session lands. Reading the chapters side by side
surfaced eleven open questions: eight rules questions for Deighton (CQ1–CQ7,
CQ10) and three CRB fixes for Ken (CQ8: Gear's Conditions table disagrees with
054's on five rows; CQ9: Dying has no table row; CQ11: 053's worked attack
example has *enemy* armor rolling PROT while the rule beside it says enemy
armor is static).

**Also answered.** F11's "Intuition Advantage" is in the **Quick Study Major
Milestone** (General Milestones, 041), not in the Advantages chapter — which is
why Ken couldn't find it there.

**Verified.** `npm run verify` 103/0/0 before and after. No engine, schema or
UI change.

**Versions.** None moved. App stays 0.11.0.

### 2026-09-22 — Conditions: catalog, Pain, skill penalties, sheet and print (Decisions 95–96)

**Why.** Ken signed off the combat plan's §3 at the start of the session: P1–P7
and P9 as written, and P8 renamed from "Reset" to **"Turn Reset"** so it reads
as an encounter helper. He asked for Session 2 with the listed stubs standing in
for the open questions.

**What changed.** Game data 0.7 → 0.8 adds `conditions` (the 19 rows of 054's
table, plus Dying), `bodyLocations` and `conditionRules`. Character schema
0.7 → 0.8 adds `trackers.conditions`. It also lands the plan's
`trackers.massiveLevels`/`witheringDamage` and
`armor[i].worn`/`scrapped`/`upgrades` now, so Session 3 needs no second
migration. Nothing reads those five fields yet.
In the engine: `conditionState()`; `painState()` now folds Condition Pain in
(Agonized) and clamps it; `skillLine()` sums a Condition's flat penalty into
the total and reports it separately; and `addCondition`/`removeCondition`/
`setConditionMarks` enforce "you have it or you don't". In the sheet: chips on
Main, full cards on Trackers, a Helpless banner, Death Mark pips, a vitals
pill, and a Conditions tick list on the print sheet's front page.

**Three stubs, three new flags.** F20 (CQ1): the −1 reaches Skill Checks only.
F21 (CQ2): different Conditions stack, with no cap. F22 (CQ3): one
location-bearing Condition can sit on two body parts. Each lives on a
`conditionRules` object with a `playerNote`. Trackers shows that note only when
a Condition governed by it is active, so a clean sheet carries no noise.

**Two design calls made in-session, worth knowing:**
- **Death Marks are a generic `counter`, not a Dying special case.** The first
  draft keyed on `id === "dying"`; replaced before it shipped, because "adding a
  Condition is a data edit" has to hold for the next counter too.
- **Attack/defense and conditional penalties are shown, not summed.** Blinded
  −5 and Prone −3 apply to attack/defense rolls, and Frightened −1 applies
  "while the source is present". The sheet knows neither context, so it labels
  them "not in the totals" instead of guessing.

**The print page nearly broke again.** The first Conditions card (a two-column
list, 203px) pushed the landscape front page past one sheet. That's the same
failure the stacked health ladder hit in Decision 94. I measured it by
force-enabling `print.css` in the browser. The shipped card is 150px: body-part
Conditions and Dying (with its Death Marks inline) sit on full-width rows so
there's room to write "left arm" in pen. The page holds at its 720px minimum
blank and filled. On a filled sheet only about 2px are spare, so anything more
added to that column will start a second page.

**Also fixed while looking at it:** the Trackers Pain card showed PL 2's
description, "(5+ HL lost)", beside "2 HL lost" when Agonized made up the
difference. It now says "Health Levels lost put you at Pain Level 1;
Conditions add +1."

**Verified.** Before: `npm run verify` 103/0/0. After: 124/0/0, with 21 new
tests: 6 CRB pins in `rules.test.mjs` (054's PL 3 example, Agonized at 0/1/3,
the clamp, no self-stacking, Helpless, three Death Marks), 10 in
`engine.test.mjs`, and 5 jsdom smoke tests that drive the real UI (add, refuse
a duplicate, undo, banner, marks, print ticks). There's also a "junk
conditions" case in the totality guard. I mutation-tested 13 guards against
the new code, 7 engine and 6 UI; each fails at least one test when its line
is removed. I checked by eye in the browser pane: Main and Trackers render,
with no console errors. The pane stopped drawing partway through, so the print
card was checked by measurement, not by screenshot.

**Versions.** App 0.11.0 → 0.12.0 · game data 0.7 → 0.8 · character schema
0.7 → 0.8.

### 2026-09-22 — Design-team rulings folded in (Decision 97)

**Why.** Later the same session, Ken brought back answers from Scott and
Deighton to part of the standing question packet.

**What changed.**
- **Closed with no behaviour change:** F1 (LUCK costs 1 CP at creation), F2
  (boosts are 1 CP per point across skills, stats and powers), and F17
  (Long-Lived's ranks stack; it's creation-only). The app already did all
  three, so only the flags and their player notes came out.
- **F14 moved a price.** Learning a new skill after creation is now a flat
  25 IP, stored as `ip.skillIncreaseCost.newSkill`. The app used to charge the
  rank-1 price (5 IP, or 3 if Focused). At creation, 0 → 1 still costs 1 Skill
  Point, as before.
- **CQ8 agreed.** The Conditions and Recovery chapter is the master document,
  and Gear's table becomes a pointer to it. That's a CRB edit; the app already
  follows it.
- **F8 stays open.** The designers want to playtest realistic Stat Point
  totals first.

**A literal reading, flagged back to Ken rather than guessed:** the ruling says
"new skills are 25", so a Professional's Focused skill at rank 0 is also 25.
Their 3× rate still applies from rank 1 up. If the designers meant Focused
skills to be cheaper, that's one data value to change.

**Versions.** The 25 IP change is observable, but game data 0.8 hasn't shipped
yet, so it went into the 0.8 note rather than a second bump. No schema change.
App stays 0.12.0: the new price and the text are part of the same unreleased
minor bump.

**Verified.** Three new engine tests. The F14 pair fails against the old
pricing (mutation-checked). `npm run verify` ends at 127/0/0.

### 2026-09-22 — Design-team rulings, part 2 (Decision 98)

**Why.** Ken's first paste of design-team answers was cut short. This is the
rest: the stat curve, the three Conditions stubs, and the combat plan's
Session 3 questions.

**What changed in the app.**
- **Stats past 10** now follow the designers' table: 11–15 = +5, 16–20 = +6,
  then +1 for every 5 points. Before this, `statMod()` added +1 per point, so a
  12 read +6 instead of +5. The step lives in the data as
  `statRules.beyondTen.stepEvery`. Only an Arcanist's focus-boosted stats, or
  a supernatural's, can get there today.
- **F20 closed:** a Condition's penalty hits Skill Checks only, which is what
  the stub already did.
- **F21 closed:** different Conditions stack, and every penalty on one roll
  caps at −8. The engine caps the Condition sum, though the current catalog
  can't reach −8 by Conditions alone. Range and cover can, but the sheet never
  sees those, so Trackers states the cap as a "How this works" note.
- **F22 closed:** only Injured and Maimed are body-part Conditions, so
  DeSynced lost its body-part picker.
- **Siege:** the tag text now says it deals Massive damage to people too.
- **The unnumbered stat-curve flag** is closed as well.

**What didn't change yet.** Plan CQ5, CQ6, CQ7 and CQ10 (only Massive causes
Injured/Maimed; Massive HL count toward Pain and need Focused Healing plus a
replacement; no armor layering; Shock at half max HL rounded up) are recorded
in the plan and in Decision 98 for Session 3 to build. Session 3 now has no
open rules questions.

**The flat 25 IP for a new skill** is confirmed for Focused skills too, "for
now". Decision 97 was updated to say so.

**Print check.** Moving DeSynced off the full-width rows left the front-page
Conditions card at 150px. The page still holds at its 720px minimum, blank
and filled (measured in the browser).

**Versions.** Folded into the unshipped game data 0.8. No schema change; app
stays 0.12.0.

**Verified.** Five new tests: the stat table and the BOD-past-10 HP pinned in
`rules.test.mjs`, plus the −8 cap, Injured/Maimed-only and the flags staying
closed in `engine.test.mjs`. The stat-curve and cap guards each fail when
their line is reverted. `npm run verify` 132/0/0. Twelve objects in the data
still carry `flagged: true`.

### 2026-09-22 — Taking a hit: armor, Massive damage, Shock and At Zero (Decision 99)

Combat plan Session 3. I proposed the shape before building and Ken approved
it as written. The proposal raised three points.

**The hit panel had no armor to read.** Nothing writes `ch.armor[]` until
Session 4's pickers, so the planned dialog would have had every real
character take every hit unarmored. Ken took the fallback over pulling the
worn toggle forward. With no worn piece, the panel offers "armor that isn't
on the sheet yet" (a typed PROT roll and RES, plus Integrity for Massive),
treated as base Kinetic armor with no wear tracked.

**Massive levels change the HP count, not just storage.** `hlState()` is the
new single picture: damage empties from the left, Massive removes from the
right, and both count as lost. `painState()` reads it and computes exactly
what it did before when there are no Massive levels.

**A rules gap STATE had said wasn't there.** RES is defined for Kinetic,
Energy (Ablative) and Magical (Warding), and the catalog also uses Electric
and Burning. Opened **F23**: both are stubbed as Energy, and the Resistance
upgrade's 50% isn't applied. That's one grouped question for Deighton.

**Two readings, recorded in Decision 99 rather than guessed silently.**
Shock counts the levels *this hit* took, not raw damage. Scrap armor still
rolls PROT with no RES, like Compromised. 053 says scrap can't be repaired,
not that it stops protecting. The Plasteel Weave Jacket's pre-installed
Tri-Weave is taken as already in its printed INT 30. Only Tri-Weave the
player installs adds +10.

**Built.** `armorState`, `resolveHit` (pure: breakdown, patch, prompts),
`applyHit` (re-resolves from the same inputs, so a stale preview can't write
stale numbers), all read from new data (`damageTypes`, `damageCategories`,
`damageRules`, and new `armorRules`/glossary fields). The Take a hit panel on
Trackers applies as one `commit()`, so the hit, its armor wear and its
Conditions undo together. Massive levels are drawn hatched on the Trackers
track, Main's mini-track and the print ladder. "Restore a Massive level" is
the explicit action CQ6 asks for. Heal all and lowering damage trim Withering
to match.

**Verified.** Twelve CRB-pinned tests in `rules.test.mjs` cover Massive at
9/10/25 with and without a vest, AP, Compromised, the soaked hit, RES
classes, Tri-Weave, Shock at exactly half, At Zero, the Death Mark while
Dying, coverage and Headshot Defense, and 053's own Maya example (15 − 5 = 10
through, two HL, Pain 1). Eight machinery tests are in `engine.test.mjs`
(totality across degenerate characters and malformed hits, purity, one-step
undo, stale-preview safety), plus three jsdom smoke tests driving the panel.
I mutation-tested the new guards with eleven engine mutations, and every one
turned a test red. The panel was checked in the browser: breakdown, Massive
hatching, HP 20/35 and Pain 1 after a 25-point Massive hit into a vest at INT
17 (scrapped), no console errors, and no horizontal overflow at 375px.
`npm run verify` 155/0/0. Fifteen objects in the data carry `flagged: true`
(twelve, plus three F23 entries).

**Versions.** App 0.12.0 → 0.13.0 (new capability). Game data stays at the
unshipped 0.8. Character schema unchanged, since 0.8 already carried every
field.

### 2026-09-22 — PR #26 adversarial review folded in (Decision 99 amended)

A fresh-context review of PR #26 posted six findings. I checked all six
against the code, and all six were real.

- **Fixed, a correctness bug (1).** With Headshot Defense, a Massive hit
  aimed at the head resolved as a torso hit, but Injured/Maimed was still
  recorded on the head. `applyHit` now places a body-part Condition where
  the hit landed, whatever the caller passes, and the panel's label uses the
  same location. It's pinned in `rules.test.mjs`, and the guard fails
  against both ways of getting it wrong.
- **Fixed, altitude (2, 3).** How a damage category resolves is now data
  on the category: `bypassesArmor`, `recordsWithering` and `inflicts`
  (Injured/Maimed moved there from `damageRules.massive`). No code compares
  against `"massive"` any more. A synthetic category in `engine.test.mjs`
  proves it, the same pattern as the Batch 3 fixtures.
- **Fixed, simplification (5, 6).** `resolveHit` is split into its stages
  (`hitArmor`, `hitLocation`, `mitigateBypass`/`mitigateArmor`,
  `hitPrompts`) ahead of Session 4. Shared `byId` and `nonNegInt` helpers
  replace the six copied lookups and the seven copied clamps, including the
  two older ones in `addCredits`/`migrate`.
- **Declined, efficiency (4).** The review said each keystroke re-renders
  the whole sheet. The handler is `onchange`, which fires on blur or Enter,
  not per keystroke. A full Trackers re-render on a committed field is what
  every other control on the sheet already does. It's a few milliseconds
  with no perceptible cost, and a panel-only render path would be a second
  way to draw the same tab. The same reasoning covers `armorState` being
  computed twice per render. Worth revisiting only if the sheet ever
  measurably lags.

**Verified.** Fifteen engine mutations: the four new guards plus the
original eleven re-expressed against the refactored code. Every one turned a
test red. In the browser, a helmeted Massive head shot now shows and stores
"Maimed (torso)". `npm run verify` 157/0/0. No version moves: the fix lands
before 0.13.0 ships.

### 2026-09-22 — v0.13.0 tagged and deployed

Tagged the PR #26 merge on `main` as `v0.13.0`. `deploy-demo` and `release`
both ran on the tag, and `charactersheet.shadowsrpg.com` now serves app
0.13.0 (checked against the live page's `APP_VERSION`). STATE had still
said the live site was on 0.9.0, which went stale when `v0.10.1` deployed
this morning. It's corrected now. Game data 0.8 is now shipped, so the
next change a character can observe bumps `gamedataVersion`.

### 2026-09-22 — Loadout & recovery (combat plan Session 4, Decision 100)

I proposed the session before building it. Ken settled four questions:

- The hit panel's stand-in armor goes. Custom armor on Loadout replaces
  it, and it was a second way to describe armor that tracked no wear.
- **Buy** sits next to Add, and pays from Çredits.
- Weapon mods and ammo tracking wait. Both need schema 0.9.
- A Nanomed Kit button waits until this lands.

Ken also moved Thick Skin and natural armor to a cleanup session after this
one.

**What was built.**

- **Engine.** `weaponLine`, `addLoadout` (with `buy`), `addCustomLoadout`,
  `removeLoadout`, `setWorn` (one per slot), `upgradeOptions` /
  `addUpgrade` / `removeUpgrade`, `armorWear` (with Self-Healing),
  `repairArmor`, `naturalHealing`, `heal`, and `resolveReset` /
  `applyReset`.
- **Refactor.** `hitPrompts` was split so Turn Reset shares At Zero and the
  Death Mark with a hit, without Shock. 054 asks Shock of "a single hit".
- **App.** Loadout pickers, weapon lines and armor rows. Main shows the
  weapons and the worn armor. Trackers gets Turn Reset, Rest, Focused
  Healing and an Armor card with After the fight. Print fills Defense and
  adds an Armor table.
- **Removed.** The bare "Restore a Massive level" button is folded into
  Focused Healing, because CQ6's ruling is Focused Healing *and* a
  replacement.

**A reading I nearly overturned.** I took Session 3's `armorPiece`, which
ignores a manufacturer-installed upgrade, for a bug and made the Plasteel
Weave Jacket 40 Integrity. At commit time I found the Session 3 log entry:
reading its built-in Tri-Weave as already in the printed INT 30 was
deliberate. It had never reached Decision 99's text. I put it back to 30
and recorded it in Decision 100 as open for Ken. The catalog doesn't
settle it, since the Nightshade is also High quality and 30 without
Tri-Weave. Lesson: grep the log, not just the ledger, before calling
earlier behavior a bug.

**New stub, F24.** When Bleeding ticks at a Reset while Dying, is that one
Death Mark or the WILL check plus a mark per source? 054 says both "an
automatic failure and a mark" and "another mark". The stub is one mark per
ticking source, standing in for the check. It goes to Deighton with F23.

**New CRB doc fixes.**

- **CQ12:** 054 and Gear disagree on whether a Nanomed Kit clears Paralyzed.
- **CQ13:** 054's table lets a week of downtime clear Injured; 055 says it
  takes Focused Healing.

**Print measured, not guessed.** With `print.css` force-enabled, a
full-width Armor table on the blank Loadout page needed 104px against about
68px spare, and pushed the sheet to a fourth page. Beside Gear it adds
nothing, and all three pages hold at 720px, blank and filled. The front
page only fills blanks that were already there.

**Tooling.** Another session held port 8420. `tools/devserver.mjs` now
honors `PORT`, and `.claude/launch.json` sets `autoPort`, so two worktrees
can preview side by side. 8420 is still the default.

**Verified.**

- **Tests.** Eleven CRB pins are in `rules.test.mjs`: 053's BOD 5 +
  BOD+3 = 8, and the attack as the skill check. 055's "a Health Level a
  day", Massive and Injured only through Focused Healing, the wear die by
  difficulty, the repair kit and scrap, Self-Healing's roll and cap, and
  upgrade slots and quality with Plasteel at 30 as read. Dying and At Zero at a
  Reset. Eight machinery tests are in `engine.test.mjs`: totality, the
  catalog weapons all resolving, Buy's one-step undo, wearing per slot, the
  stand-in's removal, the Integrity clamp, the Withering trim, and Reset
  re-resolving. Five jsdom smoke tests cover the flows, and the Massive
  smoke test now restores through Focused Healing.
- **Mutation testing.** Seven engine mutations, reverting each guard, and
  every one turned a test red. One of them guarded the Plasteel change, which
  is now reverted, so six still apply.
- **Browser.** I bought a vest and a pistol and added a knife and a helmet.
  Weapon lines read Handguns 1d10 + 9 and the knife 8 (BOD+3). Then Turn
  Reset, a soaked hit (−1 Integrity), Hard wear (a 9 refused on a d8),
  Rest two days (+10), and Focused Healing clearing Injured. A repair kit,
  an armorer, Tri-Weave (30 Integrity), a Massive hit and a Focused
  restore, then undo. Every step audited, no console errors.
- **Phone width.** At 375px the weapons table overflowed the page (539px).
  It now scrolls in its own container.
- **Suite.** `npm run verify` 181/0/0. Sixteen objects in the data carry
  `flagged: true`: fifteen, plus F24.

**Versions.** App 0.13.0 → 0.14.0 (new capability). Game data 0.8 → 0.9
(new recovery choices a character can observe). Character schema unchanged: a custom
armor piece's optional `coverage` reads as light when absent, which is what
every reader already did.

### 2026-09-22 — Plasteel ruled at 30 (Decision 100)

Ken ruled on the question PR #28 raised: the Plasteel Weave Jacket's
built-in Tri-Weave is already in its printed INT 30. His reasoning was to
start from the catalog as printed and tune numbers later if play says so.
Decision 100 records it as a ruling rather than an open reading. The code
didn't change (it was already 30); only the comments, the test's note and
STATE moved. `npm run verify` 181/0/0.

### 2026-09-22 — v0.14.0 tagged and deployed

Tagged the PR #28 merge on `main` (`e51db97`) as `v0.14.0`. `deploy-demo`
and `release` both ran on the tag, and `charactersheet.shadowsrpg.com` now
serves app 0.14.0 with game data 0.9 (checked against the live page). Game
data 0.9 is now shipped, so the next change a character can observe bumps
`gamedataVersion`. STATE records the deploy and points the board row at
#28.

### 2026-09-22 — The demo site was blank: build dropped print.css's media gate (0.14.1)

Ken opened `charactersheet.shadowsrpg.com` after the v0.14.0 deploy and got
the blue background and nothing else.

**Cause.** Decision 94 (2026-09-21) added `#app, #vscrim, #vdrawer, #footer
{ display:none }` to `print.css`, which is correct only because `index.html`
loads it with `media="print"`. `tools/build.mjs` inlined every stylesheet as
a bare `<style>` and dropped the `media` attribute. The built single file,
which is both the demo site and the file players get, hid its own app on
screen. The first tag after Decision 94 was `v0.13.0`, so the demo has been
blank since that deploy this morning. The dev server serves `index.html`,
where the gate still worked, so every browser check looked fine.

**Why nothing caught it.** jsdom ignores `media` and has no layout, so the
smoke tests boot and click through a page no browser would show. Both deploy
checks (v0.13.0 and v0.14.0, the second one mine) curled the live page for
`APP_VERSION`, which proves the file arrived, not that it renders.

**Fix.** The build carries a `<link>`'s `media` onto its `<style>`. A new
`build.test.mjs` test checks that every gated local stylesheet in
`index.html` comes out of the build with the same `media`, and that no
stylesheet hiding `#app` reaches the screen ungated. It fails against the
old build code. I opened both built artifacts (`dist/shadows-character-sheet.html`,
`dist/shadows-blank-sheet.html`) in a real browser: `#app` is `grid`, Home
renders, and the blank sheet shows its three pages. App 0.14.0 → **0.14.1**
(a visible fix). No data or schema change. `npm run verify` 182/0/0.

**Practice from here:** check a deploy by loading the page in a browser, not
by grepping it.

### 2026-09-22 — v0.14.1 tagged, deployed, and seen rendering

Tagged the #30 merge on `main` (`daa789c`) as `v0.14.1`. `deploy-demo` and
`release` both passed. Checked the way the #30 entry says to: loaded
`charactersheet.shadowsrpg.com` in a browser. It serves app 0.14.1 and game
data 0.9, `#app` is visible, Home renders, New character opens the wizard,
and there are no console errors. The demo is back after being blank since
the v0.13.0 deploy. Ken's comment: we'd been "living in our tests".

### 2026-09-22 — CI actions onto Node 24, and a manual deploy trigger

GitHub's runners flagged `actions/checkout@v4` and `actions/setup-node@v4` as
Node 20 actions being forced onto Node 24. Bumped every action to its current
major: checkout v7, setup-node v7, upload-pages-artifact v5, deploy-pages v5,
action-gh-release v3. All of them run on Node 24. I read the release notes
for each jump, and none of the breaking changes reach us:

- setup-node's automatic caching needs a `packageManager` field we don't
  have, and we already set `cache: npm`.
- upload-pages-artifact v4 drops dotfiles, and our site is `index.html`,
  `blank-sheet.html` and `CNAME`.
- checkout's credentials file and its fork-PR block don't touch our
  triggers.

The Node 22 the app builds on is unchanged. The `ubuntu-latest` → Ubuntu 26
notice needs nothing: the jobs only run Node.

**Manual trigger.** `deploy-demo` gains `workflow_dispatch`. The
`github-pages` environment accepts `main` as well as `v*` tags, so a bare
manual trigger would have let an untagged `main` reach the live site,
against the workflow's own rule. The deploy job now runs only when
`github.ref_type == 'tag'`:

- **On a branch**, a manual run is a dry run: test, build and package the
  site, no deploy.
- **On a tag**, it re-publishes that tag.

The deploy and release bumps can't be proven by a PR (they run on tags).
A manual run on `v0.14.1` after merge exercises the whole deploy path, and
re-publishes an identical build.

### 2026-09-22 — A wishlist, the TOL ruling queued, and the board moves out of STATE

Docs only. Nothing a player sees changes, so no version moves.

**`WISHLIST.md`** (Decision 101) holds ideas nobody has scheduled, as `W` ids.
Ken's seven: tracker layout, interactive vitals pills and Main cards, weapon
and armor catalog search, Loadout section shortcuts, Take a hit as a modal,
and light-mode primary buttons. Claude added six after driving the running app
with a test character: HL boxes outside the Damage card, the damage stepper
running backwards from its display (`+5` takes `25/25` to `20/25`), undo where
the action happened, a fight view, Conditions as chips, and a hit that lands.
Two findings were checked in the app, not just read:

- The light-mode bug is every `.btn.primary`, including the wizard's Continue
  and Home's Open sheet. The rule never sets a text colour, so the text
  inherits near-black `--text`.
- The catalog dropdown shows name and price only. Players pick weapons and
  armor without seeing their stats.

**Deighton's TOL ruling:** TOL = 1 + INT + BOD + COOL, where it was
INT/COOL/EMP. WILL is confirmed unchanged. It's queued in the combat plan's
cleanup session, not built. It reverses Decision 93's note, which called
INT/BOD/COOL a misreading after Scott confirmed the old formula. Ken looped
Scott in, and Scott is updating the CRB. Kept off this branch on purpose: it
bumps game data, and it shouldn't ride on a PR titled "wishlist".

**The board moved to `log/shipped.md`.** Adding the TOL line took STATE to 200
lines and failed the length guard. Rather than squeeze prose to fit, the batch
board went to `log/shipped.md` with history's other files, and STATE dropped
to about 180 lines. The same session found `CHANGELOG.md` still headed
"Unreleased — app 0.7.0" when the app is at 0.14.1. That's noted here, not
fixed.

**Decision hygiene (Decision 102).** Ken asked whether the code-level
decisions should go to an archive now that the ledger is past a hundred.
Claude argued against it. The numbers are cited from code and tests, so an
archive only moves text. And a split by importance would have buried
Decision 93, the entry whose side note caught the TOL reversal. Ken agreed.
The real problem was entries that read as current after a later decision
replaced them. Seven were in that state (14, 18, 21, 27, 32, 58, 99), and
only two said so anywhere. Each now carries a `Superseded [in part] by`
marker in SCHEMA and INDEX, and a new `docs.test.mjs` check holds the two
together. It was mutation-tested five ways (drop either marker, unstrike 21,
cite 21 as load-bearing, mismatch whole and part), and all five failed as
they should. INDEX gains a load-bearing table and a Housekeeping group.
`CLAUDE.md` gains Ken's house rule: search the ledger for what a new
decision touches before numbering it. Suite: 183 passing.

### 2026-09-23 — Combat cleanup: TOL, Natural Armor, the Nanomed Kit (0.15.0)

The combat plan's cleanup session, on `claude/session-4-cleanup-7d811a`.
Three decisions, one version bump: game data 0.9 → 0.10, app 0.14.1 → 0.15.0.

**Deighton's TOL ruling (Decision 103)** landed as planned: a data-only swap
of EMP for BOD in `derived.TOL.inputs`, with Decision 9 and Decision 93
marked superseded in part. Nothing had tested the formula before this. The
new pin in `rules.test.mjs` is the CRB's own worked example with BOD in EMP's
seat, and both new tests fail against the old inputs.

**Natural Armor (Decision 104)** needed a rules call before it could be
built. The CRB grants it four times and never says what it does to a hit.
Claude put three options to Ken: stub it in the resolver, display only, or
hold for Deighton. Ken took the stub, flagged as F25. The reasoning was that
display-only leaves the hit panel quietly over-damaging a Thick Skin
character, which is worse than a stated guess. The stub reads the two
sentences the CRB does give. "Unaffected by Armor Piercing" becomes: AP
doesn't get past it. "Treated as Warding" implies a damage class, so it
answers Kinetic by default. Iron Shirt and the waning moon are conditional:
shown, never summed, ticked on the hit panel.

The new "junk grant sources" degenerate character in the totality guard
**found a pre-existing crash**: a `null` in `advantages[]` got through
`migrate()` and threw in `grants()`, `advSpent()` and most readers
downstream. `migrate()` now drops non-entries from the held lists, as it
already did for Conditions. The guard came first; the crash was what it
turned up.

**The Nanomed Kit (Decision 105)** was blocked on CQ12. Ken answered it in
session: 054's list is the master, so the kit clears Paralyzed, and Gear's
entry is the one that changes. It's a third kind in `heal()`, not a new
writer. It proposes floor(BOD / dose) HP, and the player can lower it.

**Weapon mods and ammo** stayed out at Ken's call and went to the wishlist
as W16, still one schema 0.9 bump when someone picks it up.

Every new guard was mutation-tested: seven mutations across the natural-armor
stage, rank scaling, the Nanomed list, the dose, and `migrate()`'s filter, and
each one failed at least one test. The UI was driven in the browser pane with a
test True Warrior (Thick Skin 2, Shake it Off, kevlar vest): a hit with Iron
Shirt on, an AP hit, the Nanomed panel at doses 1 and 2, apply, and one undo
back. The print Defense card read Nat 7 on every location. Screenshots timed
out (the pane wasn't drawing), so the checks were read from page text. Suite:
190 passing.

Later the same session, Ken asked whether Scott's CRB edit had landed.
Converting the live `.docx` files showed it's in `041_Archetypes` ("INT and
COOL feed TOL … BOD feeds both") and `Magic` (the formula line), and both
were re-pulled into the mirror. `020_Synergy_System` and
`040_Character_Creation`, including the worked example the new rules test
adapts, still say INT/COOL/EMP, and their files haven't changed since July.
Magic's other changes are editorial (duplicated text trimmed, a pointer to
Copying from a Grimoire), and none moves a number the data carries.

Ken then updated `020` and `040`. The first pass swapped COOL for BOD and kept
EMP, so both files read INT + BOD + EMP, which is WILL's formula. The `040`
example also still labelled its −1 as COOL. Claude caught it by converting and
diffing before re-pulling, and held the re-pull. The second pass was right:
`020` reads "INT, BOD, and COOL", and `040` has a new example, INT 4 (0) +
BOD 3 (−1) + COOL 9 (+3) = TOL 3. All four files are re-pulled. The rules test
now pins the book's example as written instead of Claude's adaptation of the
old one. It adds EMP 9 so the old formula (which would give 7) can't pass.

### 2026-09-23 — v0.15.0 tagged, deployed, and seen rendering

PR #34 merged, and `v0.15.0` was tagged on the merge commit (`2abc0b2`).
`deploy-demo` and `release` both passed. The live page was loaded rather than
curled, as STATE asks: it reports app `0.15.0` and game data `0.10`,
`derived.TOL` reads INT/BOD/COOL, `Engine.naturalArmor` exists, Home renders
and the wizard opens at step 1. STATE now names 0.15.0 as live.

### 2026-09-23 — The combat plan closes, and a wishlist pass (0.16.0)

Ken opened by asking where the cleanup session had landed. It had merged as
#34 with its box already ticked, and `v0.15.0` was tagged and deployed that
afternoon. STATE still said "not tagged yet", so the demo site was loaded to
check. Home rendered on game data 0.10, and STATE now says 0.15.0 is live.

**The Magic-tables side session (Decision 106)** was the plan's last box.
Before building, Claude flagged that data alone would reach no player, since
nothing renders `spellcraftRules`. Ken chose the middle option: the tables,
two engine lookups, and a Cascade helper that writes its result into Notes.
Tracking acquired Aberrations as data (a schema bump) was left out.

The helper was proposed "on the Tolerance Load tracker", and that premise was
wrong. **Tolerance Load is the Cyborg's panel.** The first data edit put the
Cascade hooks on the Cyborg, and the new test caught it by failing to find
the panel on the Arcanist. The real finding: after Decision 93 removed the
old `exhaustion` panel, an Arcanist had no way to track TOL at all. The fix is
a generic `tol-spent` tracker on the Arcanist. It's data only and stored in
`trackers.panel`, so the schema doesn't move. The decision records that this
fills 93's gap rather than reversing it.

The Cascade Table starting at 3 looked like a gap for 1d10 + a degree-1
Rupture. It isn't one, because you can't cast at 0 TOL, so a Rupture that
drives TOL below zero is at least degree 2. The engine explains that instead
of guessing a row, and nothing was flagged. The Appendix's 39 Aberrations
were parsed from the mirror. The shell ate a heredoc's backslashes twice
along the way, and in the end both scripts went through a written file.

**Wishlist (Decision 107).** Ken picked W12, W11 and W14 on top of W7 and W18.
- **W7:** the guard came first, and running it found six defects, not the one
  the wishlist named: primary buttons and their hover in both themes, the
  active form toggle, stepper hover, and `::selection`. One `--on-accent`
  constant fixed all six. The guard fails the old CSS on each of them.
- **W12:** the smoke test's stale-toast case broke the first version. A seq
  check isn't enough, because `recordAction` gives the next action the same
  seq once an undo pops the last one. Matching by entry identity fixed it,
  and dropping that check fails the test.
- **W14:** the palette's body-part chip toggled its "where?" row, so retrying
  after a refused duplicate part closed the row. Now it always opens and
  Cancel closes it. The existing smoke helpers moved to the chips.
- **W18:** the CHANGELOG is caught up by tag from v0.9.0 to v0.15.0, and the
  untagged 0.11/0.12 are folded into v0.13.0. It stays a per-release file,
  and `docs.test.mjs` now fails without a heading for the current version.
  That was mutation-tested.

Also noticed and left for Ken: the Arcanist's creation-time Unique Aberrations
have drifted from `041` (Aethereal Link now says animals, Thaumaturgical
Sight adds "when analyzing magic"). It's in STATE §5. SCHEMA's own header
still said schema 0.7 and data 0.7, and was fixed in passing.

Browser checks in the preview pane: an Arcanist at TOL showed Exhausted, and
+1 opened the Cascade panel. 6 + 3 gave Permanent Aberration, the Aberration
die gave Neutral, Dense Frame went into Notes, and one undo took it back.
Light mode showed white text on violet. The toast appeared and undid, and the
palette greyed out held Conditions. Suite: 202 passing, 0 todo.

### 2026-09-23 — v0.16.0 tagged, deployed, and seen rendering

PR #36 merged, after a merge from `main` to resolve docs conflicts with #35.
`v0.16.0` was tagged on the merge commit (`a6db624`), annotated like
`v0.15.0`. `release` passed and attached the single-file build.
`deploy-demo`'s build job passed, but its deploy job failed at
`actions/deploy-pages`: "Failed to get ID Token … Request timeout". The log
blamed `id-token: write`. That permission hadn't changed since `v0.15.0`
deployed the same day, so the timeout was taken as transient and the failed
job re-run on its own. It passed.
The live page was loaded: app `0.16.0`, game data `0.11`, five Cascade rows,
39 Aberrations, the Arcanist's `tol-spent` panel, `Engine.cascade` and the
undo toast present. Home rendered and the wizard opened at step 1. The
CHANGELOG's Unreleased heading is now `v0.16.0`, as its header says a tag
does.

### 2026-09-23 — Magic on the sheet: a plan, and the Grimoire reads the book (0.17.0)

Ken loaded Wren Calloway (`tests/TestCharacters/`, a local file outside git)
after v0.16.0 and saw nothing new for magic. That was right. Loaded into the
current build, she showed only the TOL Spent tracker. The 96 spells and the
39 Cascade Aberrations were data no screen read. Looking also turned up two
strings: the Arcanist description and the wizard both pointed players at a
"Magic section" the app doesn't have, and the Grimoire note narrated the
build ("a catalog can slot in here later"). The voice test didn't catch the
second.

Claude proposed four pieces. Ken took all four, with schema 0.9 for magic
only (W16/W17 bump on their own) and a plan doc first:
`plans/magic-on-the-sheet.md`, M1–M10 and MQ1–MQ3 for Deighton. Session 1
was built the same day (Decision 108). It supersedes Decision 25 in part, and
that decision's "a catalog slots in without touching the sheet" was wrong.

Choices worth knowing:
- Book rows store only `{ spellId, stage, notes }`. `migrate()` tags typed
  rows as the player's own and never links them, the rule 0.6 used for
  weapons. The same bump seeds `trackers.aberrations`, so magic takes one
  migration.
- The first `grimoire()` created the row list if it was missing. A reader
  that writes can show up as a phantom change in the audit trail, so it
  became a pure read. A mutation test holds it there.
- Spell Attack (Evocation + REF + WILL) was left out. The book doesn't say
  whether REF is the score or its modifier. It's on STATE's Deighton list.
- Loading Wren found what the tests hadn't. A search for "fire" offered to
  **Add** Kindle, a spell she had already typed in herself. The picker now says
  **Link yours** there, and a smoke test pins it.

Seven mutations (TH not lowered, rows not tagged, the reader writing, Link
dropping notes, a held spell offered to link, Mastery not setting the stage,
Spell Power ignoring the rank) each failed a test. The browser pane wouldn't
paint deep on a long page. A 1100×2300 emulated viewport showed the whole
Loadout tab, where the search box also turned out to be unstyled (fixed).
Suite: 212 passing.

### 2026-09-23 — v0.17.0 tagged, deployed, and seen rendering

PR #38 merged, and `v0.17.0` was tagged on the merge commit (`c601f6c`),
annotated like the tags before it. `release` and `deploy-demo` both passed
first time. The OIDC timeout that hit `v0.16.0` didn't come back. The live
page was loaded: app `0.17.0`, game data `0.12`, schema `0.9`, the
Arcanist's `grimoire` panel type, and `Engine.grimoire` offering to link a
typed "Zap" after `migrate()`. Home rendered and the wizard opened. The
CHANGELOG's Unreleased heading is now `v0.17.0`.

### 2026-09-23 — Deighton's magic rulings (Decision 109)

Ken took the magic plan's three questions and Spell Attack's REF to Deighton,
and brought back all four answers the same day. They were recorded before any
code used them, as the combat plan did with Decisions 97–98, so the plan's
next session starts cold with the answers in the files.

Two answers needed a follow-up, and Ken settled both. MQ1 said starting
spells are "gated by Evocation rank" without a threshold. Ken confirmed a
spell's TH can't exceed Evocation rank, so the power level's rank cap does
the gating. MQ2 said Drained can go below 1. Asked how far, Ken explained the
model: Drained lowers the *maximum* and leaves *current* alone. That matters
because the sheet stores TOL Spent, so recording Drained has to take 2 off it
too, or current would fall with the maximum. Worked through three cases
before recording it.

The rulings override the floor of 1 in Decisions 9 and 103 in part, and both
are marked. Decision 108's held-back Spell Attack now points at 109. Still
open, and recorded as open: a maximum below 0, and what removing Drained does
to current TOL. Session 2 asks before building removal. Session 3 is
unblocked.

Ken then settled both of the open points in the same session. Max TOL is never
below 0. Removing Drained grants no TOL: max goes back up, current stays, and
the character rests the difference back. So TOL Spent goes up by 2 on removal,
mirroring the −2 on recording. Decision 109 and the plan were updated before
the branch merged, and nothing about Drained is open now.

### 2026-09-23 — Aberrations on the character, and the Magic reference (Decision 110)

The magic plan's Session 2. Proposed first, and Ken answered the proposal's
three questions: one instance of an Aberration at a time, like a Condition;
sync the Unique Aberrations to `041`, since Scott has been making those
edits; and nothing on Main for now, until playtesting says otherwise. He
expects the Origins subtypes to replace the creation-time Unique Aberrations.

Ken asked where the Unique Aberrations drift came from before answering. It
wasn't two documents disagreeing. The data matched `041` as first mirrored
(`a883f51`). The 2026-09-23 re-pull (`c78b266`) brought Scott's rewording
into the mirror and not into the data. Two of the three changes narrow a
power (Aethereal Link to animals, Thaumaturgical Sight to analyzing magic),
which is why it waited for Ken rather than being copied over.

Schema 0.9 had seeded `trackers.aberrations`, so no migration. The one
design point worth recording: Drained's "current TOL doesn't move" became a
single rule, shifting every tracker on `max: "TOL"` by the change in max,
floored at 0. That covers all four of Ken's examples, recording and removing
alike, and keeps a character already past zero past zero. The floor at 0 is
written so it never lifts a TOL that a manual adjustment already took lower.

Mutation-tested with a runner that swaps each guard for a broken version and
runs the engine, rules and smoke suites: 13 mutants, all killed. Browser
check in the preview pane on a Heroic Arcanist past TOL: 6 + 4 then 9 gave
Bad, and Record it with Drained took max TOL from 2 to 0 while current
stayed at −1. It wrote the note, recorded the Aberration and offered one
Undo. The palette added Night Eyes as a temporary chip and greyed it out.
Phantom Pain as permanent put Main at "+1 Lv from Phantom Pain". The
Archetype tab listed both permanent ones and the five reference sections,
the Grimoire showed Spell Attack 10 (Evocation 2 + REF 7 + WILL 1), and the
console stayed clean. Screenshots timed out in the pane this session, so the
check was by DOM and text. Suite: 226 passing.

### 2026-09-23 — v0.18.0 tagged, deployed, and seen rendering

PR #41 merged (`50196f5`), and `v0.18.0` was tagged on the merge commit,
annotated like the tags before it. `release` and `deploy-demo` both passed.
The live page was loaded. The footer reads `0.18.0`, and the data reads game
data `0.13` and schema `0.9`. The Arcanist declares the `reference` panel,
`recordCascade`, `aberrationState` and `spellAttack` are there, and recording
Drained on a fresh Heroic Arcanist took TOL from 2 to 0. Home rendered.
(This entry reached `main` late. #42 carried it, stayed unmerged while
Sessions 3 onward landed from another workstation, and was merged up to
`main` on 2026-09-24. By then the CHANGELOG heading and the Session 2 row in
`shipped.md` it had also carried were already on `main`, so only this entry
remained.)

### 2026-09-23 — Starting spells in the wizard, and the spell picker as a modal (Decision 111)

The magic plan's Session 3, its last. Proposed before building, and the
proposal moved M6 off the Archetype step. The plan put starting spells "on
the Arcanist's step", but both of M6's inputs are only final on step 7:
Evocation ranks are bought there (Decision 19), and step 7's boosts move INT,
BOD and COOL, which make up TOL. Picking on step 4 would have let a pick go
invalid three steps later. Ken agreed, then made two calls of his own.

**The picker became a modal.** Ken asked for a focused picker that could be
reused. It is the first user of the focus-and-dismiss primitive STATE had
been saying W2/W3/W6 wanted, which was the stronger reason to build it now.
It's a native `<dialog>` with `showModal()`. One wrinkle: a modal makes the
page behind it inert, and that includes the W12 undo toast, so the toast
moves inside an open modal and back out on close. jsdom has no
`showModal()`, so the primitive falls back to the `open` attribute and
handles Esc itself. Focus returning to `<body>` also had to be excluded as
an "opener": jsdom never focuses a clicked button, and neither does Safari.

**The TH cap is for a balanced start.** Ken first described TH above
Evocation as castable only on an exploding 10, and I read that as
reopening Deighton's MQ1. Ken clarified: creation rules often don't gate
play (skill caps are the same), so the cap applies until lock, and after
lock any spell can be learned. That arithmetic became a marker instead of
a rule, **Beyond your pool**, which counts Mastery's TH − 1.

The data's `commonSpells: "TOL + 1d4"` was a formula stored as a string,
and nothing could compute from it. It became `startingSpellsRoll: "1d4"`,
with the rule in `spellcraftRules.startingSpells`, so game data went to
0.14. Picks are the Grimoire's own rows, so there's no schema bump. Found
along the way: `resetArchetypeChoices` never cleared `panelData`, so a
wizard archetype switch now does. Also found stale: STATE said Session 2
was unmerged (it was #41), and `shipped.md` had no rows for #40 or #41.
Both are fixed.

Mutation-tested with the Session 2 runner shape: 16 mutants. The first run
left one alive, "the pool marker ignores Mastery". My test ran at Evocation
2, where a Mastered TH 2 spell is within the pool either way. At Evocation
1 the test kills it. Browser check in the preview pane on a Heroic
Arcanist, where TOL 2 plus a roll of 5 made 7:
- The dialog was truly `:modal`.
- Firebolt was open at Evocation 2 and Chain Lightning was gated with "Needs Evocation 3".
- Buying the rank opened Chain Lightning. Un-buying it kept the pick, marked it over, and put the error on the step.
- On the sheet, the add toast sat inside the modal, Undo worked from there, and the toast left the modal when it closed.
- All 14 Superior spells could be learned and were marked Beyond your pool.
- The footer read 0.19.0, and the console stayed clean.

Screenshots timed out in the pane again, so the check was by DOM and text.
Suite: 237 passing.

### 2026-09-23 — v0.19.0 tagged and live; a correction

Ken merged #43 and asked for the tag. `v0.19.0` is an annotated tag on the
merge commit (`edf72ac`), the same shape as `v0.18.0`. `verify`, `release`
and `deploy-demo` all passed, and the release carries the single-file build.
Loaded on `charactersheet.shadowsrpg.com`: the footer read app 0.19.0 and
data 0.14, and a Heroic Arcanist's step 7 showed Starting spells — TOL + 2d4
and the picker.

**A correction to the entry above.** Session 3 said nothing since 0.17.0
was tagged, so the next tag would ship Sessions 2 and 3 together. That was
wrong. `v0.18.0` had been tagged and deployed on #41's merge commit while
the session ran, and the session never fetched tags. The same claim went
into STATE and #43's body. STATE is fixed here, the entry above stays as
written (the log is append-only), and both CHANGELOG sections now carry
their tags. The lesson: fetch tags before saying what's live.

### 2026-09-23 — Ken's testing pass: W19–W26, and W19 fixed (app 0.19.1)

Ken tested v0.19.0 and brought eight items, which went into the wishlist as
W19–W26. W8 and W9 were never used, and ids aren't reused. Two needed more
than transcription:

- **W19 (Martial Arts "breaks the select")** was reproduced in the preview
  before writing it up. It turned out to be layout, not the picker. The
  Skills step's `.alloc` is a three-column grid with `display: contents`
  rows, and `picksHtml` drops the style picker in as a plain grid child, so
  it took one 194px cell and shifted every later skill one column over. Ken
  said fix it now. The fix is one rule, `.alloc > .picks { grid-column: 1 /
  -1 }`. A smoke test reads the computed `grid-column` on the real Skills
  step (jsdom computes the cascade even without layout). It fails with the
  rule removed and passes with it. In the preview, Martial Arts and Melee now
  line up to the pixel. App 0.19.1, since a player can see the difference.
- **W26 ("accept" in the modal)** had two readings. Picks already save as
  they're made, so the options were a Done button, or staged picks with
  Accept/Cancel, which would change how the picker commits and how undo
  groups. Ken chose the Done button.

W21 records that a skill's primary stat adds its score and the synergy stat
its modifier (030), so "COOL +1 syn" reads as a bonus. Suite: 238 passing.

### 2026-09-23 — v0.19.1 tagged and live

`v0.19.1` is an annotated tag on #45's merge commit (`2d75d8d`), and
`verify`, `release` and `deploy-demo` all passed. Loaded on
`charactersheet.shadowsrpg.com`: the footer read app 0.19.1 and data 0.14.
On the live Skills step, Martial Arts' style picker computed `grid-column:
1 / -1`, and Melee's name and stepper lined up with Martial Arts' own.

### 2026-09-23 — The magic plan wrapped up; three wishlist passes (app 0.20.0)

Ken asked to wrap up the magic-on-the-sheet plan and work through wishlist
items. The plan was already closed (header, SCHEMA roadmap, STATE), so
wrapping it up meant three loose ends: INDEX's plans row still named the
combat plan as in flight, and the batch board had no rows for #43 (Session 3)
or #45 (W19 and the W19–W26 pass). Both rows were appended.

Ken picked three passes, which shipped as Decision 112:

- **The modal pass (W6, W23–W26).** `openModal` gained a footer. Take a hit
  moved into the modal, with Apply disabled and its reason beside it, which
  replaced the alerts on Apply (Decision 99 in part). The spell picker got
  row clicks, dimmed rows that say why, a sticky head and Done.
- **The skill line (W20, W21).** `skillStatsHtml` shows the stat icons with
  the character's numbers on the wizard's name line and in the sheet's
  breakdown.
- **Trackers' layout (W1, W10).** Two columns from 1000px. The HL track is
  in the Damage card, grouped into the print sheet's Pain Level bands, with
  yours lit.

Three things surfaced only by running the preview, not by the tests:

- **The dialog had never been centred.** The stylesheet's `*{margin:0}`
  reset zeroes the auto margins a native `<dialog>` centres with. So 0.19's
  spell picker has opened pinned to the top-left since it shipped. The fix
  is one `margin:auto`.
- **A `type=number` field can't have its caret put back.** The hit modal
  redraws on every keystroke, so typing 9 then 2 gave "29". The wizard's
  roll fields had met this already and use `inputmode="numeric"` text. The
  hit fields do the same now, and the smoke test checks the caret.
- **Redrawing on `change` would have eaten a click on Apply.** `change`
  fires on blur, so the mousedown on Apply would have redrawn the footer
  under the pointer and dropped the click. Number fields redraw on `input`
  instead, so a blur changes nothing. In the preview, typing the PROT roll
  and clicking Apply straight away landed the hit.

Mutation-tested: 13 mutants, each failing a test. In the first batch run,
one mutant (the hit field losing focus) looked as if it had survived. The
batch's grep had missed its line. Run on its own, it failed as it should.
Suite: 242 passing.

### 2026-09-23 — STATE trimmed; F11 closed; W5 and W22 (still app 0.20.0)

STATE had reached 197 of its 200 lines, mostly write-ups of finished work
that `log/` and the ledger already hold. It went to 162 lines, with a §6
rule to keep that narration in `log/`.

Ken asked what in STATE §5's "next" list could be done tonight. **F11 turned
out to be a live bug, not a doc question.** The mirrored 041 asks for the
Intuition *skill* at Rank 1. The data had carried the WIP's "Intuition
Advantage" as an advantage prerequisite since Phase 1, and no advantage has
that id, so no character could ever take Quick Study. A rules test failed on
the old data before the fix. It's data only (Decision 113, game data 0.15).
041's new Take 6 parenthetical ("Treat the d10 as X") wasn't copied in; it
reads like a placeholder. F9 still needs Ken or Deighton, though 041 now
files General Milestones under Professional, and F12 waits on the unwritten
Advancement Section.

**W5 and W22** (Decision 114) share one helper: headings register as they
render, so the Arcanist's Disciplines and Grimoire got jumps with no app
change. Step 7's bar sticks, filters, and keeps the CP left in view. That
last one wasn't in W22, but it's the number you need while scrolling that
page. In the preview the first jump measurement read the heading far off
screen. The smooth scroll was still running, and after 2.5 s the heading sat
8px under the bar. On a phone the header wraps to 101px, which is why boot
measures it rather than using `--header-h`. The bar takes three rows there.
Suite: 245 passing.

### 2026-09-23 — v0.20.0 tagged and live

`v0.20.0` is an annotated tag on #47's merge commit (`0a8780d`), and
`release` and `deploy-demo` both passed, with the single-file build
attached to the release. Loaded on `charactersheet.shadowsrpg.com`: the
footer read app 0.20.0 and data 0.15. Quick Study's prerequisite was the
Intuition skill, and the new code (the hit modal, the jump bars) was
present. A `dialog.modal` computed `margin: auto`, so it's centred.

### 2026-09-24 — Scott's finished magic pass; the GM runs the Cascade (Decision 115, app 0.21.0)

Scott finished the Magic chapter and the spell appendix. All three magic
mirrors were re-pulled, and a parser diffed all 96 spells and 39
Aberrations field by field against the data. **The spell catalog already
matched** apart from Dominate Mind: the CRB folded its old flavor line away
and made the Sovereign Soul note its text. The parser's other hits were
pandoc's hard line breaks (Counterspell, Silence) and multi-Glyph headers,
checked by hand. What changed and went in: Force's new Domain description,
the appendix's rule that the 2x+ line applies at every tier above it,
Animal Ken reading MAG (was EMP), and Beacon working like Monster Magnet.
The rules test pinning the `as` count went from 7 to 8. It's display text
only, so game data stays 0.15 (Decision 68).

Not carried in: the priced Tools of the Trade (now W27). Also a
contradiction left for Scott. Concentration still says a Talisman holds a
spell for 1 die fewer, but the rewritten Enchantment section says an
inscribed spell runs on a duration and isn't held. The data keeps the first
rule until he says.

**The Cascade** (Decision 115). Scott told Ken the GM resolves it: the
player rolls 1d10 + the Rupture's degree and tells the GM, and the GM says
what it left. Ken also wanted to read the Aberrations before picking, for
the dread. Claude recommended dropping the dice lookup rather than keeping
it collapsed, since showing the result before the GM speaks was what killed
the dread. It also recommended a modal of cards over tap-to-preview chips,
since dread needs reading them all at once. Ken took both. The panel is now
the instruction and one button. `logCascade` and `recordCascade` are
removed. `Engine.cascade()` stays because the CRB conformance tests read
the tables through it. A Cascade no longer writes into Notes. The
Aberration's own note carries the date instead. In the preview, the side
note first rendered larger than the instruction, so the two swapped
classes. The phone width had no horizontal scroll. Suite: 244 passing (the
engine lost three tests for the two removed writers and gained two).

### 2026-09-24 (later) — AP is Armor Piercing (Decision 116)

Ken was sure AP meant Armor Piercing, and the data agreed: Iron Lance's own
effect reads "SP Damage, armor piercing", and the weapon tag of the same id
is Armor Piercing. It had been flagged since Decision 93 only because the
Magic chapter's tag list leaves it out. It's unflagged and given the weapon
tag's definition. Ken adds it to the appendix. Same PR (#49), same app
0.21.0.

### 2026-09-24 — v0.21.0 tagged and live

`v0.21.0` is an annotated tag on #49's merge commit (`d760637`), and
`release` and `deploy-demo` both passed, with the single-file build
attached to the release. Loaded on `charactersheet.shadowsrpg.com`: the
footer read app 0.21.0 and data 0.15, the Aberration picker was present,
the spell tag AP read Armor Piercing, and Beacon read as Monster Magnet.

### 2026-09-24 — The wishlist session, in the cloud (Decisions 117–122, app 0.22.0, schema 0.10, game data 0.16)

Ken's first cloud session, with credits to spend: "see how many of the
wishlist items you can do". Asked how to scope it, he picked **everything,
no pauses**, over UI-only or UI-then-propose, and one PR with one bump. So
W16, W17 and W13, which each asked for a proposal, were built on Claude's
judgment, and each decision says what was chosen and why. One commit per
item on `claude/affectionate-hawking-7zblnw`, each with its decision and a
green suite. Every guard was mutation-tested, and every UI change was
checked in headless Chromium, desktop and phone width, through
`tools/devserver.mjs`.

- **W15 (117):** `commit()` notes when damage went up, and the next render
  flashes the HP readouts and the boxes that took it. Pain beats twice when
  its level rises. It plays once and never on heal or undo.
- **W4 (118):** the catalog browser. `Engine.catalogLine` is the line
  Loadout draws, before carrying, and an engine test pins the parity for all
  54 weapons. The phone layout first ran the Add/Buy buttons off-screen, so
  on a phone each row is now a labelled card.
- **W2/W3 (119):** the vitals popovers. Doing it without a second code path
  meant moving Trackers' handlers into `bindVitalControls(root)` first. In
  jsdom, focus after a redraw landed on the damage field, since jsdom
  doesn't focus a clicked button; the test now focuses it the way a browser
  does.
- **W16 (120):** mods and rounds, schema 0.10. 053 gives Single/Burst/Full
  Auto 1/3/10 rounds. Gear gives the mods' fit, and one ambiguity (is a
  shotgun a rifle?) is **F26**, stubbed no. Sights' ACC is "aimed", kept
  apart from Single's ACC rather than summed.
- **W17/W27 (121):** the equipment catalog, 116 entries parsed from the two
  chapters' tables by script. The first pass picked up the weapon feature's
  "SMART Link" prose for the SMART Link implant, and was fixed by reading
  only the Equipment chapter. A heredoc also ate three backticked words from
  a data comment; they were caught and restored. Gear rows share weapons'
  catalog-or-custom shape in the same 0.10 bump. The shop names 13 spells
  the Book doesn't have (listed in STATE for Scott). Magic's Warding
  services became **W28**, a Deighton question.
- **W13 (122):** Main is the fight view, weapons first. No Defense number,
  since 053 rolls a skill for defense.

**GitHub refused every push** with a 403 ("Claude doesn't have GitHub access
to kazamuki/Shadows-Character-Sheet for your organization"), so the branch
exists only in the session until the Claude GitHub App's access is fixed.
Ken was told at the first refusal. Suite: 244 → 261 passing.

Later the same day, Ken reconnected GitHub and the branch pushed. The PR
opened from it.

## 2026-09-24 — Deploying from a cloud session, and What's new (Decision 123)

Ken's first cloud session merged #51 and #52 but the site stayed on 0.21.0.
Nothing was broken: `deploy-demo` only deploys a `v*` tag, the manual run was
on `main` (a dry run by design), and `v0.22.0` had never been pushed. A
cloud session can merge but usually can't push a tag. Claude tagged
`affa793` locally as `v0.22.0` (the #52 merge, so the tag carries the
renamed changelog heading); release and deploy-demo both went green, and
the live page served 0.22.0.

Ken then asked for the release notes in the app, this session. The shape
was proposed first and approved: CHANGELOG.md stays the one source, and a
generator writes a committed data script, since `file://` can't fetch the
markdown. Ken picked v0.10.1 onward (older sections are developer notes)
and the quiet home-screen line over an auto-opening window. Five lines
above the cutoff named Deighton, Scott or document 054, or said "never
tagged", and were reworded. The first run of the returning-player test
failed honestly: the Unreleased 0.23.0 section counted as newer than the
0.22.0 build, which was the missing version bump, not a bug. The second
failure was the harness's cross-realm array trap. Four guards were
mutation-tested (stale generated file, TBD in a note, pre-mark players
treated as first visits, an auto-opening window) and each failed. Checked
in the browser pane, dark and light. Suite: 261 → 266 passing.

### 2026-09-24 — Whole-app audit, and a plan to act on it (docs only)

Ken asked for a wide-net review of everything: the app, the data, the docs,
`CLAUDE.md`, and how changes get made. The instruction was to document and
change nothing. The result is `audits/2026-09-24_whole-app-audit.md`
(A4–A12, B11–B19, C4–C15, and recommended practices R1–R13) and
`plans/audit-2026-09-remediation.md` (sessions S1–S7, questions AQ1–AQ10).
Ids continue rev 9's numbering so each prefix keeps one meaning.

Most findings were run, not just read. Engine probes in the harness VM
showed that Mastery trips `versionCheck` (B11) and that three Professional
rules the CRB states are silently missing (B12–B14). A key scan of the data
against the source found the B3 class again (A9), plus about 24 KB of merged
content nothing renders (A10). A sweep of every id-bearing field found
Hardcore Parkour's prerequisite naming an Advantage 043 doesn't have (B15).
Headless Chromium, which the cloud image carries, confirmed the rest:
- a hostile `.shadows.json` ran script only in Admin mode (B16);
- a Trueborn sheet never mentions the Lunar Phase Blessing (B17);
- importing another file replaced the saved sheet with no prompt (B18);
- at 390 px the sticky header holds 30% of the screen (B19);
- offline from `file://`, the build loads in about 150 ms with system fonts.

A simulated 100-session campaign put a save file at 219 KB, so the
unbounded audit log is not a quota risk (C8). What to keep is written down
too (audit §9): the engine's shape, `file://`, and store-inputs came out
well. The macro finding is process weight. There are 8,234 lines of
hand-maintained docs against 7,612 of code, and a one-line data change
carried about 30 lines of doc edits. The drift is in exactly the places no
test reads.

Registered in `INDEX.md` §2 and pointed at from `STATE.md` §4–5. No code,
data or test changed; no version moved. Suite: 266 passing, unchanged.

### 2026-09-24 (later) — Ken's first answers to the audit; F27 (app 0.23.1, game data 0.17)

Ken answered five of the audit plan's questions the same day, all recorded
in the plan's §5:
- **AQ1:** trim the docs, as long as what was done and why stays findable.
  He asked for a way to anchor decisions against relitigation.
- **AQ3:** fix the Professional now; the data is meant to take tweaks.
- **AQ5:** design for several characters; confirm-before-replace for now,
  ideally with a download first.
- **AQ8:** Cat Like Balance is a culled Advantage from an earlier version.
- **AQ10:** tablet and laptop are the expected devices, phone the emergency.

The anchor is the plan's new §6, and it changes the entry format, not the
ledger's layout. From Decision 124, each entry records what it rejected,
when to revisit it, and a Touches line to search by. `CLAUDE.md` gets a
rule to search before proposing, and a test checks the format. Separate
decision files were considered and rejected: numbers are cited from code,
and relitigation comes from unrecorded rejections, not from the file
layout. S1 gained a stable `meta.id` (schema 0.11, proposed): without it no
replace warning or roster can tell "a newer copy" from "another
character". B19 drops to Low.

Ken asked for Cat Like Balance to be flagged, and it's **F27**. The stub
follows Decision 91: Time Sense and Danger Sense are still checked, and the
missing Advantage becomes a gold "GM:" prerequisite with the sign-off
prompt, so the Milestone can be taken. The alternative stub, locked with a
notice, would tell a player to ask their GM while the Take button stayed
disabled. Taking the Milestone is a new available choice, so game data goes
0.16 → 0.17 (Decision 68) and app 0.23.0 → 0.23.1. One `rules.test.mjs`
case pins it; it was mutation-tested and fails against the old data.
Suite: 266 → 267 passing.

### 2026-09-24 (later still) — Audit session S1: files as untrusted input (Decisions 124–127, app 0.23.1)

Ken said to start S1, and answered AQ2 (drop STATE's counts), AQ6 (embed
the fonts) and AQ9 (release from the cloud). He asked for AQ4 in plainer
words and for a view on AQ7. All of it is recorded in the plan's §5.

**B16 was much wider than the audit found.** The audit's hostile file put
payloads only in text fields. The permanent test puts them in numbers too,
and a string stored where a count lives rode the engine's `+` as text onto
every tab ("5" + 1 is "51"), with no throw. A crafted undo entry could also
write onto `Object.prototype`, confirmed in the engine's own realm (the
first check ran in the wrong one and said no). The fix went to the one
gate, not to hundreds of templates (Decision 124): `migrate()` makes every
stored number a number and drops audit entries whose path leaves the
character; undo skips such paths and re-coerces what it restores; Admin
addresses rows by position. `tests/hostile.test.mjs` walks nine tabs,
Admin, print, 60 modals and popovers, and every wizard step. It found one
more hole on its first run, the Review step's raw boost target. Six fixes
were each reverted and each reversion failed the test. Its first Admin
failure was the harness's cross-realm array trap, not the app.

The rest of S1:
- **B11:** Mastery is matched to its Grimoire row.
- **C4:** findings stay until dismissed, in the wizard too (Decision 125).
  The screenshot showed that a bare version difference would then sit on
  every page for every returning player after a data release, because a
  file keeps its old stamp until export. So that line alone still shows
  once, and the test pins both.
- **B17:** option powers render from the data, and an array of plain
  objects on a power is a table (Decision 126). "(TBD)" came out of
  `additionalPowers`; the widened voice corpus (C13) catches it if it
  returns.
- **R6, first half:** one sweep over more than 300 references, with the
  Professional's prose as named exceptions for S3 to remove.
- **AQ2**, pulled forward (Decision 127, superseding 74 in part): STATE
  states the todo count only.

124–127 are the first entries in the plan's §6 format, as a trial. Each
fits in about ten lines, against the 50–100 of recent entries.

**B18 is proposed, not built**, because it changes the save file. Reading
the lock path found a third door: making a second character replaces the
first one's live sheet. The proposal is a permanent `meta.id` (schema
0.11), `meta.updated` stamped on every change, and one confirm with
**Export first** at Import, New and Lock.

Every fix was mutation-tested. Suite: 267 → 276 tests, all passing.

### 2026-09-24 (last) — B18 as a NYTE City intake number; AQ4 and AQ7 answered (Decision 128, app 0.24.0, schema 0.11)

Ken approved the B18 proposal and gave the id an in-universe shape: an intake
form number with a barcode under the character's name. It's
`NCR-XXXX-XXXX-XXXX`, in Crockford base-32 so it reads aloud without O/0 or
I/1, from about 60 random bits, and never reissued. 2,000 new characters in
a test produce 2,000 different numbers. The bars are drawn from the number
and are decoration. A scannable Code 39 was set aside until a decoder can
verify one, rather than shipping a barcode that might not scan.

The replace guard asks at Import, New and Lock unless the incoming character
is the same one, at least as recent. That needed `meta.updated` to mean last
changed, so `commit()` stamps it. Export already stamps the file, which
makes a re-import of your own latest export silent and an older one ask.
Import now also clears the file input, so the same file can be picked again
after a Cancel.

**Print was checked, not assumed:** the front page is full (Decision 96), so
the filled and blank sheets were printed to PDF in Chromium before and after.
Both are three pages either way.

Adding the intake line to Main found **C16**: Main's subtitle read
`identity.specialization`, removed in schema 0.5, so it never named the
specialization. It's fixed, and pinned by the same test.

Six tests, and five mutations each caught: the guard never asking; a newer
copy never counting; Export first skipping its download; the old subtitle
field; the print line gone.

**AQ4** (show the rules text players can't see) is answered item by item
and specifies S6. **AQ7** was done here: the Design flag template is
deleted, the Bug template became *Bug or playtest report*, and CONTRIBUTING
and README point rules questions at SCHEMA §5. Every audit question is now
answered.
